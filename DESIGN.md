# Windows 桌面小组件 — 轻量级方案

> 常驻桌面的一小块信息面板：时间、农历、天气、今日待办。  
> **原则：能省则省 — 小体积、低内存、少依赖、单窗口、本地优先。**

---

## 0. 轻量级定义（本项目的硬指标）

| 指标 | 目标 | 不做 |
|------|------|------|
| 安装包 | **< 15 MB**（框架依赖型）或 **< 40 MB**（单文件自包含） | 不搞 Electron、不搞多模块解决方案 |
| 常驻内存 | **< 40 MB** 空闲 | 不常驻 WebView、不后台多进程 |
| 项目结构 | **单项目、单 exe** | 不拆 Core/Data/Providers 多工程 |
| 依赖 | **1 个 NuGet**（`lunar-csharp`） | 不用 EF、不用 MVVM 框架、不用 CommunityToolkit |
| 存储 | **两个 JSON 文件**（data + settings） | 不用 SQLite；扩展功能仍写进同一 data.json |
| 网络 | **仅天气** 可选联网 | 无账号、无云同步、无 OAuth |
| UI | **一种紧凑布局** + 模块开关 | 不做拖拽排版、不做皮肤市场 |
| 设置 | **设置窗口** + 精简托盘菜单 | 不做账号/云同步类重型配置 |

---

## 1. 产品定位

### 1.1 一句话

**桌面上一条「今日信息条」** — 抬头看时间农历天气，顺手勾待办，不占地方、不抢资源。

### 1.2 轻量版 vs 完整版

原先设想里的「驾驶舱」偏全功能。轻量版做减法：

| 完整版（暂缓） | 轻量版（现在做） |
|----------------|------------------|
| 月历 + 节假日调休标注 | **今日日期行** + 迷你周历（7 格） |
| Todo + 独立计划表 + 时间轴 | **合并为「今日清单」**，可选填时间 |
| 多时区、秒针动画 | **单时钟**，12/24 可切 |
| 7 日天气预报 | **今明两天** 温度 + 图标 |
| 宜忌、AQI、展开详情 | 原设计砍掉；**现实现为可选黄历详情面板**（51 万年历风格，默认开启） |
| Outlook / Google 同步 | **不做** |
| 4 种布局预设 | **1 种布局**，可调宽高 |

### 1.3 核心价值（轻量语境下）

- **几乎无感**：小块透明卡片，可置顶、可穿透
- **开机即用**：无配置也能看时间农历；天气填城市或自动定位
- **数据在本地**：一个 `data.json`，可复制备份
- **不装一堆运行时**：优先依赖系统已装的 .NET，而非自带整套 SDK

---

## 2. 功能范围（MVP 锁定）

### 2.1 唯一布局 — 紧凑竖条

```
┌─────────────────────────────┐
│  14:32        周四 6月11日    │  ← 时间 + 公历
│  农历五月初六 · 芒种          │  ← 农历 + 节气（一行）
│  ☀ 28°  晴  18°~31°         │  ← 天气（可关）
├─────────────────────────────┤
│  ▢ 买牛奶                    │  ← 今日待办（最多显示 5 条）
│  ▢ 14:00 周会               │     有时间则前缀显示
│  ▢ 写周报                    │
│  + 添加…                     │  ← 点击弹出输入，或托盘快捷键
├─────────────────────────────┤
│  一 二 三 四 五 六 日         │  ← 迷你周历（今天高亮）
│     ●                       │
└─────────────────────────────┘
     约 280 × 360 px
```

模块均可单独关闭：只要时钟、只要待办、只要天气，都行。

### 2.2 模块清单

#### 时间（必开，不可关）

- 时:分（默认不显示秒，**省定时器刷新**）
- 公历：月日、星期
- 设置：12 / 24 小时制

#### 万年历（轻量版）

- 农历月日（如「五月初六」）
- 当日节气 / 传统节日（有则显示，**一行挤下**）
- 迷你周历：当前周 7 天，今天圆点高亮
- **已实现扩展**：月历视图、翻页、黄历宜忌详情、2026 休/班标注、跳转日期
- **仍不做**：独立月历 App 级弹层、多年节假日包自动更新

#### 天气（可选模块）

- 城市、天气图标、当前温、今日高低温
- 数据源：**Open-Meteo**（免费、无需 API Key）
- 刷新：**60 分钟**一次；失败用上次缓存
- 定位：IP 粗定位或手动填城市名（**不做** GPS 持续定位）

#### 今日清单（Todo + 计划表合并）

- 字段：`标题` + 可选 `时间(HH:mm)` + `完成`
- 只显示**今天**的条目；无日期或日期=今天的都进今日
- 最多展示 5 条，超出托盘菜单「查看全部」打开小窗口（第二阶段）
- 本地 JSON 持久化，**无优先级、无标签、无子任务**

### 2.3 明确不做的功能（防止膨胀）

- 云同步、账号登录
- Outlook / Google / 微软 To Do 集成
- 重复日程规则引擎（每天/每周/每月）
- 拖拽排版、插件市场、皮肤商店
- 宜忌、独立天气详情页、7 日预报大面板
- 多语言（先只做简体中文）
- 内嵌浏览器、RSS 阅读器、股票行情

---

## 2.4 扩展功能设想（轻量可插拔）

> **原则**：以下功能全部 **默认关闭**，在托盘里按需开启；实现上优先「本地计算 / 同一 JSON / 同一天气 API」，不新增重型依赖。  
> 每个模块 UI 占用 **≤ 2 行**，可折叠，避免面板变成长条瀑布。

### 功能地图

```
                    ┌─ 感知层（看）
                    │   时间 / 农历 / 天气 / 月相 / 日出日落
                    │
  DeskLite 核心 ────┼─ 行动层（做）
                    │   今日清单 / 倒数日 / 习惯打卡 / 番茄钟
                    │
                    └─ 陪伴层（省）
                        每日一句 / 护眼提醒 / 进度条 / 快捷启动
```

### A 类 — 强烈推荐（性价比高，仍轻量）

| 模块 | 展示示例 | 实现成本 | 说明 |
|------|----------|----------|------|
| **倒数日** | `距离春节还有 214 天` | 低 | 用户自建事件（考试、发薪、纪念日、项目上线）；内置常见节日表，本地算 |
| **今年/本周进度** | `2026 已过 45.2% ▓▓▓░░` | 低 | 纯日期计算；可切换「本周已过 %」「距周末 2 天」 |
| **待办到时提醒** | 托盘 Toast：`14:00 周会` | 低 | 仅针对**今天且写了时间**的待办；到点弹一次，不搞复杂重复规则 |
| **日出 / 日落** | `日出 04:51 · 日落 19:32` | 低 | Open-Meteo 同一请求顺带返回，不增加 API |
| **月相** | `🌔 盈凸月` | 低 | 本地天文算法，一行图标+文字 |
| **每日一句** | `行胜于言。` | 低 | 内置 100～300 条本地语录，按日期种子随机，**完全离线** |
| **速记便签** | 一行可编辑文字 | 低 | `scratch` 字段写入 JSON，适合临时手机号、会议号 |
| **置顶待办** | ⭐ 标记 1 条 | 低 | 今日清单内最多置顶 1 条，始终显示在最上 |

### B 类 — 很实用（略增交互，仍可控）

| 模块 | 展示示例 | 实现成本 | 说明 |
|------|----------|----------|------|
| **习惯打卡** | `喝水 ●○○  早睡 ●●○` | 中 | 用户定义 ≤5 个习惯，每日点一下；零点自动重置 |
| **番茄钟** | `专注 18:32 ▶` | 中 | 25/5 默认，托盘显示倒计时；**不另开窗口**，折叠区展开才操作 |
| **护眼提醒** | 每 20 分钟托盘提示 | 低 | 20-20-20 规则；仅 Toast + 可选弱提示音，**不抢焦点** |
| **快捷启动** | 4 个图标/文字按钮 | 中 | 用户拖入 exe/文件夹/网址，一键打开；数据存 JSON 路径 |
| **第二时区** | `东京 15:32` | 低 | 仅 **1 个**副时钟，与主时钟共用分钟 tick |
| **节假日标注** | 周历上 `休` `班` 小字 | 中 | 国务院节假日 **年度 JSON 包**（每年更新一次内置文件），离线 |
| **查看全部待办** | 小弹窗列表 | 中 | 待办 >5 条时托盘打开；简单列表，可搜标题，无标签体系 |
| **数据备份** | 导出/导入 JSON | 低 | 托盘一键导出 `desk-lite-backup-日期.json` |

### C 类 — 锦上添花（按需开启）

| 模块 | 展示示例 | 实现成本 | 说明 |
|------|----------|----------|------|
| **生日/纪念日** | `3 天后 妈妈生日` | 中 | 与倒数日共用数据，按年重复；提前 3 天开始在面板提示 |
| **今明天气对比** | `明天 小雨 22°~26°` | 低 | 天气 API 多取一天，一行展示 |
| **UV / 降水概率** | `UV 7 · 降水 30%` | 低 | 天气 API 扩展字段，一行 |
| **电量/充电** | `🔋 76%` | 低 | 笔记本 `SystemInformation.PowerStatus`；台式机自动隐藏 |
| **下一待办倒计时** | `1h20m 后 周会` | 低 | 取最近一条未完成的定时待办 |
| **透明度/字号** | 托盘滑块 | 低 | 无设置页，滑块调节面板透明度与字体 |
| **贴边收纳** | 鼠标移开自动缩成细条 | 中 | 仅露时间一行，悬停展开；适合靠边放置 |
| **全局快捷键** | `Ctrl+Shift+N` 快速记待办 | 低 | 弹出极简输入框，回车即保存 |

### D 类 — 暂不纳入（偏重或与定位不符）

| 功能 | 不做的原因 |
|------|------------|
| CPU/内存/网速实时监控 | 需高频采样，占 CPU，违背「无感」 |
| 音乐「正在播放」 | 需对接 Spotify/WMI，脆弱且依赖多 |
| 剪贴板历史 | 隐私敏感，易误触泄露 |
| 星座运势 / 宜忌 | 争议内容，与工具定位不符 |
| 股票 / 加密货币 | 需 API Key + 高频刷新 |
| 内嵌 RSS / 新闻 | 易变 Web 内容，倾向做浏览器的事 |
| 多窗口 / 多显示器多实例 | 复杂度陡增，后期再评估 |

### 扩展后的布局示意（全开时）

```
┌─────────────────────────────┐
│  14:32        周四 6月11日  │
│  农历五月初六 · 芒种         │
│  2026 已过 45.2% ▓▓▓░░░░   │  ← 可选：年进度
├─────────────────────────────┤
│  ☀ 28° 晴  18°~31°          │
│  日出 04:51 · 日落 19:32    │  ← 可选：日出日落
│  明天 多云 20°~27°          │  ← 可选：明日天气
├─────────────────────────────┤
│  ⏳ 距离国庆还有 112 天      │  ← 可选：倒数日
│  「行胜于言。」              │  ← 可选：每日一句
├─────────────────────────────┤
│  ⭐ ▢ 14:00 周会            │
│     ▢ 买牛奶                 │
│     ▢ 写周报                 │
│  + 添加…                     │
├─────────────────────────────┤
│  喝水 ●○○  运动 ●●○        │  ← 可选：习惯打卡
│  专注 18:32 ▶               │  ← 可选：番茄钟（折叠）
├─────────────────────────────┤
│  一 二 三 四 五 六 日        │
│        ●  休                 │  ← 周历 + 节假日小标
├─────────────────────────────┤
│  [VSCode] [记事本] [+] [+]  │  ← 可选：快捷启动
└─────────────────────────────┘
```

**收纳策略**：模块超过 6 个时，建议默认只开 4 个；其余在托盘「模块管理」里勾选。面板总高度上限建议 **520px**，超出内部滚动（仅列表区滚动，头部时间固定）。

### 扩展后的数据模型（仍是一个 JSON）

```json
// data.json（扩展字段，向后兼容）
{
  "todos": [
    { "id": "1", "title": "周会", "time": "14:00", "date": "2026-06-11", "done": false, "pinned": true }
  ],
  "countdowns": [
    { "id": "c1", "title": "春节", "date": "2027-02-06", "repeatYearly": false }
  ],
  "habits": [
    { "id": "h1", "name": "喝水", "target": 3, "doneToday": 1, "lastDate": "2026-06-11" }
  ],
  "shortcuts": [
    { "id": "s1", "label": "VSCode", "path": "C:\\...\\Code.exe" }
  ],
  "scratch": "会议号 8866 002",
  "birthdays": [
    { "id": "b1", "name": "妈妈", "month": 6, "day": 14 }
  ]
}
```

```json
// settings.json（模块开关）
{
  "modules": {
    "weather": true,
    "lunar": true,
    "weekStrip": true,
    "yearProgress": false,
    "countdown": true,
    "dailyQuote": true,
    "habits": false,
    "pomodoro": false,
    "eyeCare": false,
    "shortcuts": false,
    "sunriseSunset": true,
    "moonPhase": false,
    "secondTimezone": false,
    "todoReminder": true
  },
  "secondTimezone": "Asia/Tokyo",
  "pomodoroWorkMin": 25,
  "pomodoroBreakMin": 5
}
```

### 推荐默认开启组合（「日常套餐」）

适合大多数用户的第一版配置，**不算臃肿**：

| 开启 | 关闭 |
|------|------|
| 时间 + 农历 + 周历 | 番茄钟、习惯打卡 |
| 天气 + 日出日落 | 第二时区、月相 |
| 今日清单 + 到时提醒 | 快捷启动 |
| 倒数日（春节/自定义 1 个） | 护眼提醒（默认关，用户可开） |
| 每日一句 | 年进度、电量 |

---

## 3. 交互（极简）

| 操作 | 行为 |
|------|------|
| 拖标题区 | 移动窗口 |
| 双击标题区 | 显示/隐藏「穿透鼠标」 |
| 右键标题区 | 模块开关、置顶、设置城市、退出 |
| 托盘图标 | 显示隐藏、添加待办、开机自启、退出 |
| `Ctrl+Shift+W` | 全局显示/隐藏（可选，默认关） |

**已实现独立设置窗口**（`SettingsWindow`）：常规 / 外观 / 天气 / 日历 / 模块五栏；托盘保留常用快捷操作。与原「仅托盘配置」相比，属于**有意偏离**——模块与外观项增多后，集中设置更易维护。

---

## 4. 技术方案（轻量优先）

### 4.1 推荐技术栈

| 层级 | 选型 | 轻量理由 |
|------|------|----------|
| UI | **WPF** (.NET 8) | 比 WinUI 3 依赖小；透明窗口成熟；资料多 |
| 模式 | **Code-behind + 少量 Service** | 不引入 MVVM 框架；文件少 |
| 存储 | **`%AppData%/DeskLite/data.json`** | 无需数据库 |
| 配置 | **同目录 `settings.json`** | 与数据分离 |
| 农历 / 黄历 | **lunar-csharp**（1 个 NuGet） | 黄历详情需库支持；简洁农历行仍可用 |
| 天气 | `HttpClient` + JSON 解析 | 一个类搞定 |
| 托盘 | `System.Windows.Forms.NotifyIcon` | WPF 里常用，就一个图标 |
| 发布 | **框架依赖** `win-x64` | 体积最小（用户需 .NET 8） |

**为何不用 WinUI 3（轻量语境）**

- Windows App SDK 运行时体积大，对小工具过重
- 若以后要做 Mica 精致效果，再评估迁移；MVP 用 WPF 半透明 + 圆角 `Border` 足够

**为何不用 Electron / Tauri**

- 内存通常 100MB+，与「轻量」目标相反

### 4.2 项目结构（单项目，当前）

```
DeskLite/
├── App.xaml / App.xaml.cs
├── MainWindow.xaml(.cs)       # 主面板
├── SettingsWindow.xaml(.cs)   # 设置窗口
├── TodoListWindow.xaml(.cs)   # 全部待办（已实现，待接入入口）
├── TrayService.cs             # 系统托盘
├── InputPrompt.cs
├── Models/
│   ├── AppSettings.cs         # settings.json
│   ├── AppDataFile.cs         # data.json 根
│   ├── TodoItem.cs / TodoDisplayItem.cs
│   ├── CountdownItem.cs
│   ├── WeatherCache.cs
│   └── DeskModuleIds.cs       # 模块 ID 与默认顺序
└── Services/
    ├── JsonStore.cs
    ├── LunarCalendar.cs / HuangLiService.cs
    ├── WeatherService.cs / LocationService.cs
    ├── TodoStore.cs / TodoReminderService.cs
    ├── CountdownService.cs / YearProgressService.cs
    ├── DailyQuoteService.cs / HolidayService.cs
    ├── CalendarViewHelper.cs
    ├── AppTheme.cs / FontScaleHelper.cs / WindowHelper.cs
    ├── GlobalHotkeyService.cs / AutoStartService.cs
    └── AppIconService.cs
```

当前约 **30+ 源文件**（仍单项目单 exe）。未实现项（习惯、番茄钟等）**未提前建空壳**。

### 4.3 数据模型（最小）

```json
// settings.json
{
  "city": "北京",
  "showWeather": true,
  "showLunar": true,
  "showWeekStrip": true,
  "time24h": true,
  "alwaysOnTop": true,
  "clickThrough": false,
  "autoStart": false
}
```

```json
// data.json
{
  "todos": [
    { "id": "1", "title": "周会", "time": "14:00", "date": "2026-06-11", "done": false }
  ]
}
```

### 4.4 性能策略

- 时钟：**每分钟**更新 UI 一次（不显示秒）
- 天气：启动拉一次，之后每 60 分钟；窗口激活时不额外刷新
- 农历：日期变更时算一次，缓存当天结果
- 不用 `DispatcherTimer` 堆多个，**一个定时器**管时钟

---

## 5. 体积与内存估算

| 发布方式 | 大约体积 | 说明 |
|----------|----------|------|
| 框架依赖 `dotnet publish -c Release` | **~2–5 MB** exe + 依赖 DLL | 需用户安装 [.NET 8 桌面运行时](https://dotnet.microsoft.com/download) |
| 自包含单文件 `PublishSingleFile` | **~35–45 MB** | 免装运行时，适合「解压即用」 |

| 状态 | 内存目标 |
|------|----------|
| 空闲展示 | **25–40 MB** |
| 刚启动 | **< 2 秒** 出界面 |

---

## 6. 路线图与实现状态

> **审计日期**：2026-06-11 · 对照 `DeskLite/` 源码与 git 历史

### 6.1 功能对照表

| 功能 | 设计预期 | 实际状态 | 备注 |
|------|----------|----------|------|
| WPF 透明窗口 + 拖拽 | Phase 1 | ✅ 已完成 | 无边框、`AllowsTransparency` |
| 时间 + 公历 | Phase 1 | ✅ 已完成 | 可选显示秒 |
| 农历一行 | Phase 1 轻量 | ✅ 已完成 | 黄历关闭时显示简洁农历行 |
| 今日待办 JSON | Phase 1 | ✅ 已完成 | 增删勾、可选时间 |
| 托盘显示/隐藏/退出 | Phase 1 | ✅ 已完成 | 双击托盘切换 |
| 天气 Open-Meteo | Phase 2 | ✅ 已完成 | 60 分钟刷新 + 缓存 |
| 迷你周历 | Phase 2 | ✅ 已完成 | 扩展：月历、翻页、跳转 |
| 置顶 / 穿透 / 自启 | Phase 2 | ✅ 已完成 | 设置窗口 + 右键菜单 |
| 框架依赖发布 + README | Phase 2 | ✅ 已完成 | |
| 倒数日 + 内置节日 | Phase 3 批 1 | ✅ 已完成 | 自定义事件 + 进度条 |
| 待办到时 Toast | Phase 3 批 1 | ✅ 已完成 | |
| 每日一句 | Phase 3 批 1 | ✅ 已完成 | 离线语录 |
| 日出 / 日落 | Phase 3 批 1 | ✅ 已完成 | 同天气 API |
| 年进度 | Phase 3 批 1 | ✅ 已完成 | 仅年进度，**无**周进度/距周末 |
| 速记便签 | Phase 3 批 1 | ✅ 已完成 | |
| 模块开关 | Phase 3 批 1 | ✅ 已完成 | **设置窗口**勾选，非仅托盘 |
| 置顶待办 | 扩展 A 类 | ✅ 已完成 | 数据层支持，主面板星标前缀 |
| 节假日休/班 | Phase 3 批 2 | ✅ 已完成 | **仅 2026** 离线表 |
| 数据导出 | Phase 3 批 2 | ⚠️ 部分 | 托盘导出到桌面；**无导入** |
| 查看全部待办 | Phase 3 批 2 | ⚠️ 部分 | `TodoListWindow` 已实现，**未接线**、未提交 |
| 透明度 / 字号 | 扩展 C 类 | ✅ 已完成 | 设置滑块：30–100%、10–16 pt |
| 四边缩放 | — | ✅ 已完成 | `WindowHelper.EnableBorderlessResize` |
| 模块显示顺序 | — | ✅ 已完成 | 设置 → 模块 → 上移/下移 |
| 黄历详情（宜忌等） | 原设计不做 | ✅ 已完成 | **偏离**：51wnl 风格大面板，可关 |
| 设置窗口 | 原设计不做 | ✅ 已完成 | **偏离**：五栏 Tab 集中配置 |
| 深/浅色主题 | — | ✅ 已完成 | |
| 自动定位城市 | — | ✅ 已完成 | 启动 + 24h |
| 全局快捷键 | 扩展 C 类 | ✅ 已完成 | Ctrl+Shift+D / N |
| 习惯打卡 | Phase 3 批 2 | ❌ 未实现 | |
| 番茄钟 | Phase 3 批 2 | ❌ 未实现 | |
| 快捷启动 | Phase 3 批 2 | ❌ 未实现 | |
| 数据导入 | Phase 3 批 2 | ❌ 未实现 | |
| 贴边收纳 | Phase 3 批 3 | ❌ 未实现 | |
| 第二时区 / 月相 / 电量 | Phase 3 批 3 | ❌ 未实现 | |
| 护眼提醒 | Phase 3 批 3 | ❌ 未实现 | |
| 自包含单文件打包 | Phase 3 批 3 | ❌ 未实现 | 仍框架依赖优先 |
| 生日/纪念日、UV、下一待办倒计时 | 扩展 B/C | ❌ 未实现 | |

### 6.2 与原设计的主要偏离

1. **黄历详情**：轻量版原砍掉宜忌；现默认展示完整黄历面板（`lunar-csharp` + `HuangLiService`），可关闭回简洁农历行。
2. **设置入口**：由「托盘为主、无设置页」改为 **SettingsWindow** + 精简托盘；外观（透明度/字号 pt）、模块顺序均在此配置。
3. **日历能力**：超出「迷你周历」，支持月历、锚点翻页、托盘跳转日期。
4. **文件规模**：约 30+ 源文件，略超原「25 文件」软上限，仍保持单项目。

### 6.3 Phase 勾选（路线图）

#### Phase 1 — 能用的最小版

- [x] WPF 无边框透明窗口 + 拖拽
- [x] 时间 + 农历一行
- [x] 今日待办 JSON 增删勾
- [x] 托盘：显示隐藏、退出

#### Phase 2 — 完整轻量版

- [x] 天气 Open-Meteo + 缓存
- [x] 迷你周历（含月历扩展）
- [x] 置顶 / 穿透 / 开机自启
- [x] 框架依赖发布 + 简单 README

#### Phase 3 — 扩展模块

**第一批**

- [x] 倒数日 + 内置节日
- [x] 待办到时 Toast 提醒
- [x] 每日一句（离线语录）
- [x] 日出 / 日落（天气 API 顺带）
- [x] 年进度（**未做**周进度 / 距周末）
- [x] 速记便签（scratch 一行）
- [x] 模块开关

**第二批**

- [ ] 习惯打卡（≤5 个）
- [ ] 番茄钟（折叠区）
- [x] 节假日 `休/班` 年度包（2026）
- [ ] 查看全部待办小窗 + 搜索（代码已有，待接入）
- [ ] 快捷启动（4 格）
- [ ] 数据导出 / 导入（仅导出）

**第三批**

- [ ] 贴边收纳
- [x] 透明度 / 字号滑块（设置窗口）
- [ ] 第二时区、月相、电量
- [ ] 护眼提醒
- [ ] 自包含单文件打包

### 明确延后（非轻量范围）

- WinUI 3 重写、Outlook 同步、插件市场、系统监控大图

---

## 7. 已拍板（轻量版默认）

结合「想要轻量级」的偏好，以下项**默认确定**，除非你另说：

| 项 | 决定 |
|----|------|
| 产品名 | **DeskLite**（暂定） |
| 技术 | **WPF + .NET 8** |
| 打包 | **框架依赖** 优先（最小体积） |
| 天气 | **Open-Meteo**，无需 Key |
| 宜忌 | **不做** |
| 同步 | **不做**，纯本地 JSON |
| 系统 | **Windows 10/11**（WPF 通用） |
| 布局 | **一种紧凑竖条** |

---

## 8. 风险（轻量版）

| 风险 | 对策 |
|------|------|
| 用户没装 .NET 运行时 | README 写明下载链接；或提供自包含 zip |
| Open-Meteo 国内慢 | 超时 5s，显示缓存；可选备用源（后期） |
| WPF 透明窗口偶发毛边 | 用 `AllowsTransparency` + `WindowChrome` 常规方案 |
| 功能越加越多 | 本文「不做清单」作边界，新需求先问是否必要 |

---

## 9. 下一步（基于当前实现）

1. ~~Phase 1 / 2~~（已完成）
2. ~~Phase 3 第一批~~（已完成）
3. **近期优先**：接入 `TodoListWindow` 入口、数据导入、2027+ 节假日数据
4. **第二批按需**：习惯打卡、番茄钟、快捷启动
5. **体验项**：贴边收纳、自包含打包

---

## 10. 功能优先级投票（历史记录）

> 初稿征集用；多数 A 类与 B5 已实现，见 §6.1 对照表。

| 编号 | 功能 | 状态 |
|------|------|------|
| A1 | 倒数日 | ✅ |
| A2 | 年/周进度 | ⚠️ 仅年进度 |
| A3 | 待办到时提醒 | ✅ |
| A4 | 日出日落 | ✅ |
| A5 | 每日一句 | ✅ |
| A6 | 速记便签 | ✅ |
| B1 | 习惯打卡 | ❌ |
| B2 | 番茄钟 | ❌ |
| B3 | 护眼提醒 | ❌ |
| B4 | 快捷启动 | ❌ |
| B5 | 节假日休/班标注 | ✅（2026） |
| B6 | 查看全部待办 | ⚠️ 未接入 |
| C1 | 贴边收纳 | ❌ |
| C2 | 第二时区 | ❌ |
| C3 | 月相 | ❌ |
| C4 | 明日天气对比 | ✅ |

---

*文档版本：v0.4-implemented · 更新：2026-06-11（实现审计同步）*
