<Window x:Class="DeskLite.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="Floatly 设置"
        Width="1520"
        Height="980"
        MinWidth="1180"
        MinHeight="760"
        ShowInTaskbar="True"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        ResizeMode="CanResize"
        Background="#0B111A"
        Foreground="#FFFFFF"
        FontFamily="Microsoft YaHei UI"
        FontSize="13">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="0"
                            ResizeBorderThickness="6"
                            CornerRadius="0"
                            GlassFrameThickness="0"
                            UseAeroCaptionButtons="False" />
    </shell:WindowChrome.WindowChrome>

    <Window.Resources>
        <ResourceDictionary>
        <SolidColorBrush x:Key="SettingsBg" Color="#0B111A" />
        <SolidColorBrush x:Key="SettingsShellBg" Color="#F0121A28" />
        <SolidColorBrush x:Key="SettingsSidebarBg" Color="#C0111927" />
        <SolidColorBrush x:Key="SettingsCardBg" Color="#C21A2332" />
        <SolidColorBrush x:Key="SettingsCardBorder" Color="#1EFFFFFF" />
        <SolidColorBrush x:Key="SettingsText" Color="#F7FAFF" />
        <SolidColorBrush x:Key="SettingsMuted" Color="#9EACC0" />
        <SolidColorBrush x:Key="SettingsInputBg" Color="#F01A2435" />
        <SolidColorBrush x:Key="SettingsBorder" Color="#32FFFFFF" />
        <SolidColorBrush x:Key="SettingsAccent" Color="#4D82FF" />
        <SolidColorBrush x:Key="SettingsAccentDim" Color="#234D82FF" />
        <SolidColorBrush x:Key="SettingsGreen" Color="#55D38A" />
        <SolidColorBrush x:Key="SettingsRecord" Color="#F05252" />
        <SolidColorBrush x:Key="SettingsTitleBarBg" Color="#B80D1521" />
        <SolidColorBrush x:Key="SettingsFooterBg" Color="#B80D1521" />
        <SolidColorBrush x:Key="SettingsShellBorder" Color="#26FFFFFF" />
        <SolidColorBrush x:Key="SettingsDivider" Color="#14FFFFFF" />
        <SolidColorBrush x:Key="SettingsSecondaryBg" Color="#B51D2736" />
        <SolidColorBrush x:Key="SettingsSecondaryHoverBg" Color="#C21F2A3D" />
        <SolidColorBrush x:Key="SettingsSecondaryPressedBg" Color="#D71A2333" />
        <SolidColorBrush x:Key="SettingsToggleOffBg" Color="#2A3140" />
        <SolidColorBrush x:Key="SettingsToggleOffBorder" Color="#33FFFFFF" />
        <SolidColorBrush x:Key="SettingsCheckboxBg" Color="#1C2737" />
        <SolidColorBrush x:Key="SettingsCheckboxBorder" Color="#5A6A7F" />
        <SolidColorBrush x:Key="SettingsSliderTrack" Color="#354154" />
        <SolidColorBrush x:Key="SettingsRadioBg" Color="#162131" />
        <SolidColorBrush x:Key="SettingsRadioBorder" Color="#6A778D" />
        <SolidColorBrush x:Key="SettingsListBg" Color="#5C172234" />
        <SolidColorBrush x:Key="SettingsListBorder" Color="#18FFFFFF" />
        <SolidColorBrush x:Key="SettingsListItemBorder" Color="#12FFFFFF" />
        <SolidColorBrush x:Key="SettingsListSelectedBg" Color="#223149" />
        <SolidColorBrush x:Key="SettingsListHoverBg" Color="#1B2A40" />
        <SolidColorBrush x:Key="SettingsInnerPanelBg" Color="#30141D2B" />
        <SolidColorBrush x:Key="SettingsTitleIcon" Color="#D8E5FF" />
        <SolidColorBrush x:Key="SettingsWarning" Color="#FFB86C" />
        <SolidColorBrush x:Key="SettingsPreviewBg" Color="#121820" />
        <SolidColorBrush x:Key="SettingsPreviewBorder" Color="#44FFFFFF" />

        <Style x:Key="CardTitle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="{DynamicResource SettingsText}" />
            <Setter Property="Margin" Value="0,0,0,14" />
        </Style>
        <Style x:Key="FieldLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource SettingsMuted}" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>
        <Style x:Key="InfoGlyph" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe MDL2 Assets" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Foreground" Value="{DynamicResource SettingsMuted}" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>
        <Style x:Key="HintText" TargetType="TextBlock">
            <Setter Property="FontSize" Value="11" />
            <Setter Property="Foreground" Value="{DynamicResource SettingsMuted}" />
            <Setter Property="TextWrapping" Value="Wrap" />
        </Style>

        <Style x:Key="SettingsCard" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource SettingsCardBg}" />
            <Setter Property="BorderBrush" Value="{DynamicResource SettingsCardBorder}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="16" />
            <Setter Property="Padding" Value="18" />
            <Setter Property="Margin" Value="0,0,0,14" />
        </Style>

        <Style x:Key="SettingsTextBox" TargetType="TextBox">
            <Setter Property="Height" Value="34" />
            <Setter Property="Background" Value="{DynamicResource SettingsInputBg}" />
            <Setter Property="Foreground" Value="{DynamicResource SettingsText}" />
            <Setter Property="BorderBrush" Value="{DynamicResource SettingsBorder}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="12,0" />
            <Setter Property="CaretBrush" Value="{DynamicResource SettingsText}" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10"
                                Padding="{TemplateBinding Padding}"
                                SnapsToDevicePixels="True">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          VerticalAlignment="Stretch" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="SettingsComboBox" TargetType="ComboBox">
            <Setter Property="Height" Value="34" />
            <Setter Property="Background" Value="{DynamicResource SettingsInputBg}" />
            <Setter Property="Foreground" Value="{DynamicResource SettingsText}" />
            <Setter Property="BorderBrush" Value="{DynamicResource SettingsBorder}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="12,0,34,0" />
            <Setter Property="MaxDropDownHeight" Value="220" />
            <Setter Property="ItemContainerStyle">
                <Setter.Value>
                    <Style TargetType="ComboBoxItem">
                        <Setter Property="MinHeight" Value="30" />
                        <Setter Property="Padding" Value="10,6" />
                        <Setter Property="Foreground" Value="{DynamicResource SettingsText}" />
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ComboBoxItem">
                                    <Border x:Name="ItemChrome"
                                            Background="Transparent"
                                            CornerRadius="8"
                                            Padding="{TemplateBinding Padding}">
                                        <ContentPresenter VerticalAlignment="Center"
                                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" />
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsHighlighted" Value="True">
                                            <Setter TargetName="ItemChrome" Property="Background" Value="{DynamicResource SettingsListHoverBg}" />
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="ItemChrome" Property="Background" Value="{DynamicResource SettingsListSelectedBg}" />
                                        </Trigger>
                                        <Trigger Property="IsEnabled" Value="False">
                                            <Setter Property="Opacity" Value="0.45" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid SnapsToDevicePixels="True">
                            <Border x:Name="Chrome"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="10" />
                            <ContentPresenter x:Name="ContentSite"
                                              IsHitTestVisible="False"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}"
                                              Margin="{TemplateBinding Padding}"
                                              VerticalAlignment="Center" />
                            <TextBox x:Name="PART_EditableTextBox"
                                     Style="{x:Null}"
                                     Background="Transparent"
                                     Foreground="{DynamicResource SettingsText}"
                                     BorderThickness="0"
                                     Margin="{TemplateBinding Padding}"
                                     VerticalContentAlignment="Center"
                                     Visibility="Hidden"
                                     IsReadOnly="{TemplateBinding IsReadOnly}" />
                            <ToggleButton x:Name="ToggleButton"
                                          Width="32"
                                          HorizontalAlignment="Right"
                                          Focusable="False"
                                          IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          ClickMode="Press"
                                          Background="Transparent"
                                          BorderThickness="0">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <TextBlock Text="&#xE70D;"
                                                   FontFamily="Segoe MDL2 Assets"
                                                   Foreground="{DynamicResource SettingsMuted}"
                                                   FontSize="10"
                                                   HorizontalAlignment="Center"
                                                   VerticalAlignment="Center" />
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <Popup x:Name="Popup"
                                   Placement="Bottom"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   PopupAnimation="Fade">
                                <Border Background="{DynamicResource SettingsInputBg}"
                                        BorderBrush="{DynamicResource SettingsBorder}"
                                        BorderThickness="1"
                                        CornerRadius="10"
                                        MinWidth="{TemplateBinding ActualWidth}"
                                        MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <ScrollViewer Margin="4" SnapsToDevicePixels="True">
                                        <ItemsPresenter />
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEditable" Value="True">
                                <Setter TargetName="PART_EditableTextBox" Property="Visibility" Value="Visible" />
                                <Setter TargetName="ContentSite" Property="Visibility" Value="Hidden" />
                            </Trigger>
                            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                <Setter TargetName="Chrome" Property="BorderBrush" Value="{DynamicResource SettingsAccent}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="SettingsSlider" TargetType="Slider">
            <Setter Property="Minimum" Value="0" />
            <Setter Property="Maximum" Value="100" />
            <Setter Property="Height" Value="20" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Margin" Value="0,0,12,0" />
            <Setter Property="Foreground" Value="{DynamicResource SettingsAccent}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Slider">
                        <Grid Height="22" VerticalAlignment="Center">
                            <Border Height="4"
                                    Margin="7,0"
                                    CornerRadius="2"
                                    Background="{DynamicResource SettingsSliderTrack}"
                                    VerticalAlignment="Center" />
                            <Track x:Name="PART_Track" Margin="7,0">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="Slider.DecreaseLarge"
                                                  BorderThickness="0"
                                                  Background="{DynamicResource SettingsAccent}"
                                                  IsTabStop="False">
                                        <RepeatButton.Template>
                                            <ControlTemplate TargetType="RepeatButton">
                                                <Border Height="4"
                                                        Background="{TemplateBinding Background}"
                                                        CornerRadius="2"
                                                        VerticalAlignment="Center" />
                                            </ControlTemplate>
                                        </RepeatButton.Template>
                                    </RepeatButton>
                                </Track.DecreaseRepeatButton>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="Slider.IncreaseLarge"
                                                  BorderThickness="0"
                                                  Background="Transparent"
                                                  IsTabStop="False">
                                        <RepeatButton.Template>
                                            <ControlTemplate TargetType="RepeatButton">
                                                <Border Background="Transparent" />
                                            </ControlTemplate>
                                        </RepeatButton.Template>
                                    </RepeatButton>
                                </Track.IncreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb Width="16" Height="16">
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="Thumb">
                                                <Border Background="#F8FAFF" BorderBrush="#80FFFFFF" BorderThickness="1" CornerRadius="8" />
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="SettingsNumericBox" TargetType="TextBox" BasedOn="{StaticResource SettingsTextBox}">
            <Setter Property="Width" Value="72" />
            <Setter Property="TextAlignment" Value="Center" />
        </Style>
        <Style x:Key="CompactValueBox" TargetType="TextBox" BasedOn="{StaticResource SettingsTextBox}">
            <Setter Property="Width" Value="56" />
            <Setter Property="Height" Value="28" />
            <Setter Property="Padding" Value="4,0" />
            <Setter Property="TextAlignment" Value="Right" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Transparent" />
        </Style>
        <Style x:Key="ColorChipButton" TargetType="Button">
            <Setter Property="Width" Value="30" />
            <Setter Property="Height" Value="30" />
            <Setter Property="Margin" Value="0,0,8,0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="SettingsCheckBox" TargetType="CheckBox">
            <Setter Property="Foreground" Value="{DynamicResource SettingsText}" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="MinHeight" Value="24" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <Border x:Name="Box"
                                    Width="18"
                                    Height="18"
                                    CornerRadius="4"
                                    Background="{DynamicResource SettingsCheckboxBg}"
                                    BorderBrush="{DynamicResource SettingsCheckboxBorder}"
                                    BorderThickness="1"
                                    VerticalAlignment="Center">
                                <TextBlock x:Name="CheckGlyph"
                                           Text="&#xE73E;"
                                           FontFamily="Segoe MDL2 Assets"
                                           FontSize="10"
                                           Foreground="White"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"
                                           Visibility="Collapsed" />
                            </Border>
                            <ContentPresenter Margin="8,0,0,0" VerticalAlignment="Center" />
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Box" Property="Background" Value="{DynamicResource SettingsAccent}" />
                                <Setter TargetName="Box" Property="BorderBrush" Value="{DynamicResource SettingsAccent}" />
                                <Setter TargetName="CheckGlyph" Property="Visibility" Value="Visible" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.45" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="FloatlyToggle" TargetType="CheckBox">
            <Setter Property="Foreground" Value="{DynamicResource SettingsText}" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="MinHeight" Value="28" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <ContentPresenter Grid.Column="0" VerticalAlignment="Center" />
                            <Border x:Name="Track" Grid.Column="1" Width="38" Height="22" CornerRadius="11"
                                    Background="{DynamicResource SettingsToggleOffBg}" BorderBrush="{DynamicResource SettingsToggleOffBorder}" BorderThickness="1" Margin="12,0,0,0">
                                <Border x:Name="Thumb" Width="16" Height="16" CornerRadius="8"
                                        Background="#E9EEF7" HorizontalAlignment="Left" Margin="3,0,0,0" />
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Track" Property="Background" Value="{DynamicResource SettingsAccent}" />
                                <Setter TargetName="Thumb" Property="HorizontalAlignment" Value="Right" />
                                <Setter TargetName="Thumb" Property="Margin" Value="0,0,3,0" />
                                <Setter TargetName="Thumb" Property="Background" Value="White" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.45" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="NavItemStyle" TargetType="ListBoxItem">
            <Setter Property="Padding" Value="16,11" />
            <Setter Property="Margin" Value="10,4" />
            <Setter Property="Foreground" Value="{DynamicResource SettingsMuted}" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="Bd" Background="Transparent" CornerRadius="8" Padding="{TemplateBinding Padding}"
                                BorderThickness="2,0,0,0" BorderBrush="Transparent">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource SettingsAccentDim}" />
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource SettingsAccent}" />
                                <Setter Property="Foreground" Value="{DynamicResource SettingsText}" />
                                <Setter Property="FontWeight" Value="SemiBold" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource SettingsAccentDim}" />
                                <Setter Property="Foreground" Value="{DynamicResource SettingsText}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource SettingsAccent}" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="22,10" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="MinWidth" Value="110" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="10" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#5A8DFF" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#3E72F0" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource SettingsSecondaryBg}" />
            <Setter Property="Foreground" Value="{DynamicResource SettingsText}" />
            <Setter Property="BorderBrush" Value="{DynamicResource SettingsBorder}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="14,8" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="MinWidth" Value="80" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource SettingsSecondaryHoverBg}" />
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource SettingsAccent}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource SettingsSecondaryPressedBg}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="SecondaryToggleButton" TargetType="ToggleButton">
            <Setter Property="Background" Value="{DynamicResource SettingsSecondaryBg}" />
            <Setter Property="Foreground" Value="{DynamicResource SettingsText}" />
            <Setter Property="BorderBrush" Value="{DynamicResource SettingsBorder}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="10,7" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="MinWidth" Value="78" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="10"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource SettingsAccent}" />
                                <Setter TargetName="Bd" Property="BorderThickness" Value="2" />
                                <Setter TargetName="Bd" Property="Background" Value="#1E4D82FF" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource SettingsSecondaryHoverBg}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="LinkButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{DynamicResource SettingsMuted}" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="8,6" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="HorizontalContentAlignment" Value="Left" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="Transparent" Padding="{TemplateBinding Padding}">
                            <ContentPresenter />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Foreground" Value="{DynamicResource SettingsText}" />
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="TitleButton" TargetType="Button" BasedOn="{StaticResource LinkButton}">
            <Setter Property="Width" Value="38" />
            <Setter Property="Height" Value="30" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>
        <Style x:Key="ModuleOrderListBox" TargetType="ListBox">
            <Setter Property="Background" Value="{DynamicResource SettingsListBg}" />
            <Setter Property="Foreground" Value="{DynamicResource SettingsText}" />
            <Setter Property="BorderBrush" Value="{DynamicResource SettingsListBorder}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Disabled" />
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled" />
            <Setter Property="ItemContainerStyle">
                <Setter.Value>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="0" />
                        <Setter Property="Margin" Value="0" />
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Border x:Name="Bd" Background="Transparent" BorderBrush="{DynamicResource SettingsListItemBorder}" BorderThickness="0,0,0,1">
                                        <ContentPresenter />
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="Bd" Property="Background" Value="{DynamicResource SettingsListSelectedBg}" />
                                        </Trigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Bd" Property="Background" Value="{DynamicResource SettingsListHoverBg}" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ModuleOrderButton" TargetType="Button" BasedOn="{StaticResource SecondaryButton}">
            <Setter Property="MinWidth" Value="56" />
            <Setter Property="Height" Value="34" />
            <Setter Property="Padding" Value="8,0" />
            <Setter Property="Margin" Value="0,0,0,8" />
        </Style>
        <Style x:Key="PomodoroValueBox" TargetType="TextBox" BasedOn="{StaticResource SettingsTextBox}">
            <Setter Property="Height" Value="32" />
            <Setter Property="Width" Value="94" />
            <Setter Property="Padding" Value="12,0,42,0" />
            <Setter Property="TextAlignment" Value="Left" />
        </Style>
        <Style x:Key="HotkeyDisplayBox" TargetType="TextBox" BasedOn="{StaticResource SettingsTextBox}">
            <Setter Property="Height" Value="32" />
            <Setter Property="MinWidth" Value="136" />
            <Setter Property="Padding" Value="10,0" />
        </Style>
        <Style x:Key="UnitInputBox" TargetType="TextBox" BasedOn="{StaticResource SettingsTextBox}">
            <Setter Property="Height" Value="36" />
            <Setter Property="Padding" Value="14,0" />
        </Style>
        <Style x:Key="IconSquareButton" TargetType="Button" BasedOn="{StaticResource SecondaryButton}">
            <Setter Property="Width" Value="34" />
            <Setter Property="Height" Value="32" />
            <Setter Property="MinWidth" Value="34" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Background" Value="{DynamicResource SettingsInputBg}" />
            <Setter Property="BorderBrush" Value="{DynamicResource SettingsBorder}" />
        </Style>
        <Style TargetType="RadioButton">
            <Setter Property="Foreground" Value="{DynamicResource SettingsText}" />
            <Setter Property="Margin" Value="0,0,26,0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <Grid Width="18" Height="18" VerticalAlignment="Center">
                                <Ellipse x:Name="Ring" Stroke="{DynamicResource SettingsRadioBorder}" StrokeThickness="1.4" Fill="{DynamicResource SettingsRadioBg}" />
                                <Ellipse x:Name="Dot" Width="6" Height="6" Fill="White" Visibility="Collapsed" />
                            </Grid>
                            <ContentPresenter Margin="10,0,0,0" VerticalAlignment="Center" />
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Ring" Property="Fill" Value="{DynamicResource SettingsAccent}" />
                                <Setter TargetName="Ring" Property="Stroke" Value="{DynamicResource SettingsAccent}" />
                                <Setter TargetName="Dot" Property="Visibility" Value="Visible" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.45" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="SettingsScrollPageButton" TargetType="RepeatButton">
            <Setter Property="Focusable" Value="False" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RepeatButton">
                        <Border Background="Transparent" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ScrollBar">
            <Setter Property="Width" Value="8" />
            <Setter Property="MinWidth" Value="8" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid x:Name="Root" Background="Transparent" SnapsToDevicePixels="True">
                            <Track x:Name="PART_Track" IsDirectionReversed="True">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.PageUpCommand"
                                                  Style="{StaticResource SettingsScrollPageButton}" />
                                </Track.DecreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb x:Name="Thumb" MinHeight="32">
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="Thumb">
                                                <Border x:Name="ThumbChrome"
                                                        Margin="2"
                                                        CornerRadius="4"
                                                        Background="{DynamicResource SettingsBorder}" />
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="ThumbChrome" Property="Background" Value="{DynamicResource SettingsAccentDim}" />
                                                    </Trigger>
                                                    <Trigger Property="IsDragging" Value="True">
                                                        <Setter TargetName="ThumbChrome" Property="Background" Value="{DynamicResource SettingsAccent}" />
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.PageDownCommand"
                                                  Style="{StaticResource SettingsScrollPageButton}" />
                                </Track.IncreaseRepeatButton>
                            </Track>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Orientation" Value="Horizontal">
                                <Setter Property="Height" Value="8" />
                                <Setter Property="MinHeight" Value="8" />
                                <Setter Property="Width" Value="Auto" />
                                <Setter Property="MinWidth" Value="0" />
                                <Setter TargetName="PART_Track" Property="IsDirectionReversed" Value="False" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ScrollViewer">
            <Setter Property="Background" Value="Transparent" />
        </Style>
        </ResourceDictionary>
    </Window.Resources>

    <Border x:Name="ShellBorder" Background="{DynamicResource SettingsShellBg}" BorderBrush="{DynamicResource SettingsShellBorder}" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="44" />
                <RowDefinition Height="*" />
                <RowDefinition Height="58" />
            </Grid.RowDefinitions>

            <Grid x:Name="TitleBar" Grid.Row="0" Background="{DynamicResource SettingsTitleBarBg}" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="18,0,0,0">
                    <TextBlock Text="&#xE713;" FontFamily="Segoe MDL2 Assets" FontSize="17" Foreground="{DynamicResource SettingsTitleIcon}" Margin="0,0,12,0" />
                    <TextBlock Text="Floatly 2.0 - 设置" FontSize="15" Foreground="{DynamicResource SettingsText}" VerticalAlignment="Center" />
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,10,0">
                    <Button Style="{StaticResource TitleButton}" Click="TitleMinimize_Click" ToolTip="最小化">
                        <TextBlock Text="&#xE921;" FontFamily="Segoe MDL2 Assets" />
                    </Button>
                    <Button Style="{StaticResource TitleButton}" Click="TitleMaximize_Click" ToolTip="最大化">
                        <TextBlock x:Name="MaximizeIcon" Text="&#xE922;" FontFamily="Segoe MDL2 Assets" />
                    </Button>
                    <Button Style="{StaticResource TitleButton}" Click="TitleClose_Click" ToolTip="关闭">
                        <TextBlock Text="&#xE8BB;" FontFamily="Segoe MDL2 Assets" />
                    </Button>
                </StackPanel>
            </Grid>

            <Grid Grid.Row="1">
                <ScrollViewer x:Name="ContentScrollViewer"
                              Margin="18,16,18,0" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                    <Grid Width="{Binding ActualWidth, ElementName=ContentScrollViewer}" MaxWidth="1464" HorizontalAlignment="Center">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1.16*" MinWidth="360" />
                            <ColumnDefinition Width="16" />
                            <ColumnDefinition Width="0.88*" MinWidth="286" />
                            <ColumnDefinition Width="16" />
                            <ColumnDefinition Width="1.34*" MinWidth="428" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
<Border x:Name="SectionGeneral" Height="0" Visibility="Collapsed" />
                            <Border Style="{StaticResource SettingsCard}" Height="206" Padding="18,14,18,12">
                                <StackPanel>
                                    <TextBlock Style="{StaticResource CardTitle}" Text="窗口行为" />
                                    <Grid Margin="0,0,0,10">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="窗口置顶" Foreground="{DynamicResource SettingsText}" VerticalAlignment="Center" />
                                        <CheckBox x:Name="ChkAlwaysOnTop" Grid.Column="1" Style="{StaticResource FloatlyToggle}" />
                                    </Grid>
                                    <Grid Margin="0,0,0,10">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="开机自动启动" Foreground="{DynamicResource SettingsText}" VerticalAlignment="Center" />
                                        <CheckBox x:Name="ChkAutoStart" Grid.Column="1" Style="{StaticResource FloatlyToggle}" />
                                    </Grid>
                                    <Grid Margin="0,0,0,10">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                            <TextBlock Text="鼠标穿透（开启后无法拖动窗口）" Foreground="{DynamicResource SettingsText}" VerticalAlignment="Center" />
                                            <TextBlock Text="&#xE946;" Style="{StaticResource InfoGlyph}" Margin="8,0,0,0" />
                                        </StackPanel>
                                        <CheckBox x:Name="ChkClickThrough" Grid.Column="1" Style="{StaticResource FloatlyToggle}" />
                                    </Grid>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                            <TextBlock Text="启用全局快捷键" Foreground="{DynamicResource SettingsText}" VerticalAlignment="Center" />
                                            <TextBlock Text="&#xE946;" Style="{StaticResource InfoGlyph}" Margin="8,0,0,0" />
                                        </StackPanel>
                                        <CheckBox x:Name="ChkGlobalHotkey" Grid.Column="1" Style="{StaticResource FloatlyToggle}" />
                                    </Grid>
                                </StackPanel>
                            </Border>
                            <Border Style="{StaticResource SettingsCard}" Height="196" Padding="18,14,18,12">
                                <StackPanel>
                                    <TextBlock Style="{StaticResource CardTitle}" Text="工作时间" />
                                    <Grid Margin="0,0,0,10">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="112" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="上班时间" Foreground="{DynamicResource SettingsText}" VerticalAlignment="Center" />
                                        <TextBox x:Name="TxtWorkStartTime"
                                                 Grid.Column="1"
                                                 Style="{StaticResource SettingsTextBox}"
                                                 Width="142"
                                                 Height="32"
                                                 HorizontalAlignment="Right"
                                                 TextAlignment="Center" />
                                    </Grid>
                                    <Grid Margin="0,0,0,10">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="112" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="下班时间" Foreground="{DynamicResource SettingsText}" VerticalAlignment="Center" />
                                        <TextBox x:Name="TxtWorkEndTime"
                                                 Grid.Column="1"
                                                 Style="{StaticResource SettingsTextBox}"
                                                 Width="142"
                                                 Height="32"
                                                 HorizontalAlignment="Right"
                                                 TextAlignment="Center" />
                                    </Grid>
                                    <CheckBox x:Name="ChkOffWorkWeekdaysOnly"
                                              Style="{StaticResource SettingsCheckBox}"
                                              Content="仅在工作日显示下班倒计时" />
                                </StackPanel>
                            </Border>
                            <Border Style="{StaticResource SettingsCard}" Height="124" Padding="18,14,18,12">
                                <StackPanel>
                                    <TextBlock Style="{StaticResource CardTitle}" Text="时钟显示" />
                                    <Grid Margin="0,0,0,12">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="74" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="时间格式" Foreground="{DynamicResource SettingsText}" VerticalAlignment="Center" />
                                        <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="24,0,0,0">
                                            <RadioButton x:Name="RbTime24h" Content="24 小时制" GroupName="TimeFormat" />
                                            <RadioButton x:Name="RbTime12h" Content="12 小时制" GroupName="TimeFormat" />
                                        </StackPanel>
                                    </Grid>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="显示秒" Foreground="{DynamicResource SettingsText}" VerticalAlignment="Center" />
                                        <CheckBox x:Name="ChkShowSeconds" Grid.Column="1" Style="{StaticResource FloatlyToggle}" />
                                    </Grid>
                                </StackPanel>
                            </Border>
                            <Border x:Name="SectionWeather" Height="0" Visibility="Collapsed" />
                            <Border Style="{StaticResource SettingsCard}" Height="250" Padding="18,16,18,14">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="154" />
                                        <ColumnDefinition Width="20" />
                                        <ColumnDefinition Width="1" />
                                        <ColumnDefinition Width="20" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="*" />
                                    </Grid.RowDefinitions>

                                    <TextBlock Grid.ColumnSpan="5" Style="{StaticResource CardTitle}" Text="天气" Margin="0,0,0,12" />

                                    <StackPanel Grid.Row="1" Grid.Column="0">
                                        <CheckBox x:Name="ChkShowWeather" Style="{StaticResource FloatlyToggle}" Content="显示天气" Margin="0,0,0,10" />
                                        <CheckBox x:Name="ChkShowCityName" Style="{StaticResource FloatlyToggle}" Content="显示城市名称" Margin="0,0,0,10" />
                                        <CheckBox x:Name="ChkShowSunrise" Style="{StaticResource FloatlyToggle}" Content="显示日出日落" Margin="0,0,0,10" />
                                        <CheckBox x:Name="ChkShowTomorrow" Style="{StaticResource FloatlyToggle}" Content="显示明日天气" />
                                    </StackPanel>

                                    <Border Grid.Row="1" Grid.Column="2" Background="{DynamicResource SettingsDivider}" Width="1" Margin="0,2,0,0" />

                                    <StackPanel Grid.Row="1" Grid.Column="4">
                                        <TextBlock Text="城市定位" Foreground="{DynamicResource SettingsText}" FontWeight="SemiBold" Margin="0,0,0,10" />
                                        <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                                            <RadioButton x:Name="RbAutoLocate" Content="自动定位城市" GroupName="CityLocate" />
                                            <TextBlock Text="启动时 + 每24小时刷新" Style="{StaticResource HintText}" VerticalAlignment="Center" Margin="-10,0,0,0" />
                                        </StackPanel>
                                        <RadioButton x:Name="RbManualCity" Content="手动输入城市" GroupName="CityLocate" Margin="0,0,0,12" />
                                        <Grid Margin="0,0,0,14">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="10" />
                                                <ColumnDefinition Width="96" />
                                            </Grid.ColumnDefinitions>
                                            <TextBox x:Name="TxtCity"
                                                     Style="{StaticResource SettingsTextBox}"
                                                     VerticalAlignment="Center" />
                                            <Button x:Name="BtnDetectLocation" Grid.Column="2" Content="立即定位"
                                                    Style="{StaticResource SecondaryButton}"
                                                    Padding="0"
                                                    Height="34"
                                                    MinWidth="96"
                                                    Click="BtnDetectLocation_Click" />
                                        </Grid>
                                        <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                            <TextBlock Text="定位状态：" Foreground="{DynamicResource SettingsText}" VerticalAlignment="Center" />
                                            <Ellipse Width="8" Height="8" Fill="{DynamicResource SettingsGreen}" Margin="0,0,8,0" VerticalAlignment="Center" />
                                            <TextBlock x:Name="LocationStateText" Text="已定位" Foreground="{DynamicResource SettingsText}" FontSize="12" VerticalAlignment="Center" Margin="0,0,12,0" />
                                            <TextBlock x:Name="LocationStatusText" Style="{StaticResource HintText}" Text="Tokyo, Japan" VerticalAlignment="Center" />
                                        </StackPanel>
                                        <TextBlock x:Name="LocationIpWarningText" Style="{StaticResource HintText}"
                                                   Text="当前结果可能来自 IP 定位，建议开启 Windows 位置服务"
                                                   Foreground="{DynamicResource SettingsWarning}" Visibility="Collapsed" />
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </StackPanel>

<StackPanel Grid.Column="2">
                            <Border Style="{StaticResource SettingsCard}" Height="206" Padding="18,14,18,12">
                                <StackPanel>
                                    <TextBlock Style="{StaticResource CardTitle}" Text="快捷键设置" />
                                    <Grid Margin="0,0,0,10">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="132" />
                                            <ColumnDefinition Width="12" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="8" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="显示 / 隐藏快捷键" Foreground="{DynamicResource SettingsText}" VerticalAlignment="Center" />
                                        <TextBox x:Name="TxtHotkeyShowHide" Grid.Column="2" Style="{StaticResource HotkeyDisplayBox}" IsReadOnly="True"
                                                 PreviewKeyDown="HotkeyTextBox_PreviewKeyDown"
                                                 GotFocus="HotkeyShowHide_GotFocus" LostFocus="HotkeyTextBox_LostFocus" />
                                        <Button x:Name="BtnRecordShowHide" Grid.Column="4" VerticalAlignment="Center"
                                                Style="{StaticResource IconSquareButton}"
                                                Click="BtnRecordShowHide_Click" ToolTip="编辑">
                                            <TextBlock Text="&#xE70F;" FontFamily="Segoe MDL2 Assets" />
                                        </Button>
                                    </Grid>
                                    <Grid Margin="0,0,0,10">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="132" />
                                            <ColumnDefinition Width="12" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="8" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="快速待办快捷键" Foreground="{DynamicResource SettingsText}" VerticalAlignment="Center" />
                                        <TextBox x:Name="TxtHotkeyQuickTodo" Grid.Column="2" Style="{StaticResource HotkeyDisplayBox}" IsReadOnly="True"
                                                 PreviewKeyDown="HotkeyTextBox_PreviewKeyDown"
                                                 GotFocus="HotkeyQuickTodo_GotFocus" LostFocus="HotkeyTextBox_LostFocus" />
                                        <Button x:Name="BtnRecordQuickTodo" Grid.Column="4" VerticalAlignment="Center"
                                                Style="{StaticResource IconSquareButton}"
                                                Click="BtnRecordQuickTodo_Click" ToolTip="编辑">
                                            <TextBlock Text="&#xE70F;" FontFamily="Segoe MDL2 Assets" />
                                        </Button>
                                    </Grid>
                                    <Border BorderBrush="{DynamicResource SettingsDivider}" BorderThickness="0,1,0,0" Margin="0,0,0,10" />
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="52" />
                                        </Grid.ColumnDefinitions>
                                        <StackPanel>
                                            <TextBlock Text="录制模式" Foreground="{DynamicResource SettingsText}" Margin="0,0,0,4" />
                                            <TextBlock x:Name="HotkeyStatusText" Style="{StaticResource HintText}"
                                                       Text="点击录制后，按下新的快捷键..." />
                                        </StackPanel>
                                        <Button x:Name="BtnRecordMode" Grid.Column="1" Style="{StaticResource IconSquareButton}" Width="38" Height="34"
                                                Click="BtnRecordShowHide_Click" ToolTip="录制模式" HorizontalAlignment="Right" VerticalAlignment="Center">
                                            <Grid>
                                                <Ellipse Width="16" Height="16" Stroke="#F26A6A" StrokeThickness="2" />
                                                <Ellipse Width="6" Height="6" Fill="#F26A6A" />
                                            </Grid>
                                        </Button>
                                    </Grid>
                                </StackPanel>
                            </Border>
                            <Border Style="{StaticResource SettingsCard}" Height="334" Padding="18,14,18,12">
                                <StackPanel>
                                    <TextBlock Style="{StaticResource CardTitle}" Text="摸鱼收入助手" />
                                    <Grid Margin="0,0,0,10">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="126" />
                                            <ColumnDefinition Width="14" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="月薪设置（元）" Foreground="{DynamicResource SettingsText}" VerticalAlignment="Center" />
                                        <TextBox x:Name="TxtMonthlySalary" Grid.Column="2" Style="{StaticResource UnitInputBox}" TextAlignment="Left" />
                                    </Grid>
                                    <Grid Margin="0,0,0,10">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="126" />
                                            <ColumnDefinition Width="14" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="42" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="每月工作天数" Foreground="{DynamicResource SettingsText}" VerticalAlignment="Center" />
                                        <TextBox x:Name="TxtWorkDaysPerMonth" Grid.Column="2" Style="{StaticResource UnitInputBox}" />
                                        <TextBlock Grid.Column="3" Text="天" Foreground="{DynamicResource SettingsMuted}"
                                                   HorizontalAlignment="Right" VerticalAlignment="Center" />
                                    </Grid>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="126" />
                                            <ColumnDefinition Width="14" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="42" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="每日工作小时" Foreground="{DynamicResource SettingsText}" VerticalAlignment="Center" />
                                        <TextBox x:Name="TxtWorkHoursPerDay" Grid.Column="2" Style="{StaticResource UnitInputBox}" />
                                        <TextBlock Grid.Column="3" Text="小时" Foreground="{DynamicResource SettingsMuted}"
                                                   HorizontalAlignment="Right" VerticalAlignment="Center" />
                                    </Grid>
                                </StackPanel>
                            </Border>
                            <Border x:Name="SectionCalendar" Height="0" Visibility="Collapsed" />
                            <Border Style="{StaticResource SettingsCard}" Height="250" Padding="18,16,18,14">
                                <StackPanel>
                                    <TextBlock Style="{StaticResource CardTitle}" Text="日历" Margin="0,0,0,12" />
                                    <CheckBox x:Name="ChkShowWeekStrip" Style="{StaticResource FloatlyToggle}" Content="显示万年历" Margin="0,0,0,12" />
                                    <CheckBox x:Name="ChkShowHuangLi" Style="{StaticResource FloatlyToggle}" Content="显示黄历详情（宜忌、神煞、时辰）" Margin="0,0,0,14" />
                                    <Border Background="{DynamicResource SettingsDivider}" Height="1" Margin="0,0,0,16" />
                                    <TextBlock Text="默认视图" Foreground="{DynamicResource SettingsText}" FontWeight="SemiBold" Margin="0,0,0,12" />
                                    <StackPanel Orientation="Horizontal">
                                        <RadioButton x:Name="RbCalWeek" Content="周历" GroupName="Calendar" />
                                        <RadioButton x:Name="RbCalMonth" Content="月历" GroupName="Calendar" />
                                    </StackPanel>
                                </StackPanel>
                            </Border>
                        </StackPanel>

                        <StackPanel Grid.Column="4">
                            <Border x:Name="SectionAppearance" Height="0" Visibility="Collapsed" />
                            <Border Style="{StaticResource SettingsCard}" Height="554" Padding="18,14">
                                <StackPanel>
                                    <TextBlock Style="{StaticResource CardTitle}" Text="主题" Margin="0,0,0,10" />
                                    <Grid Margin="0,0,0,14">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="170" />
                                            <ColumnDefinition Width="14" />
                                            <ColumnDefinition Width="114" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <Border x:Name="ThemeDarkCard" CornerRadius="8" BorderThickness="2" BorderBrush="{DynamicResource SettingsAccent}"
                                                Background="#172238" Padding="12,10" Cursor="Hand" MouseLeftButtonUp="ThemeDarkCard_Click"
                                                Height="58">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto" />
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>
                                                <Border x:Name="ThemeDarkSwatch" Width="24" Height="24" CornerRadius="6" Background="#0F172A" VerticalAlignment="Center" />
                                                <TextBlock x:Name="ThemeDarkText" Grid.Column="1" Text="深色" Foreground="{DynamicResource SettingsText}" VerticalAlignment="Center" Margin="12,0,0,0" />
                                                <TextBlock x:Name="ThemeDarkCheck" Grid.Column="2" Text="&#xE73E;" FontFamily="Segoe MDL2 Assets"
                                                           Foreground="{DynamicResource SettingsAccent}" VerticalAlignment="Center" />
                                            </Grid>
                                        </Border>
                                        <Border x:Name="ThemeLightCard" Grid.Column="2" CornerRadius="8" BorderThickness="2" BorderBrush="#24FFFFFF"
                                                Background="#E8ECF1" Padding="12,10" Cursor="Hand" MouseLeftButtonUp="ThemeLightCard_Click"
                                                Height="58">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto" />
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>
                                                <Border x:Name="ThemeLightSwatch" Width="24" Height="24" CornerRadius="6" Background="#F5F7FA" VerticalAlignment="Center" />
                                                <TextBlock x:Name="ThemeLightText" Grid.Column="1" Text="浅色" Foreground="#1F2937" VerticalAlignment="Center" Margin="12,0,0,0" />
                                                <TextBlock x:Name="ThemeLightCheck" Grid.Column="2" Text="&#xE73E;" FontFamily="Segoe MDL2 Assets"
                                                           Foreground="{DynamicResource SettingsAccent}" VerticalAlignment="Center"
                                                           Visibility="Collapsed" />
                                            </Grid>
                                        </Border>
                                        <RadioButton x:Name="RbThemeDark" GroupName="Theme" Visibility="Collapsed" />
                                        <RadioButton x:Name="RbThemeLight" GroupName="Theme" Visibility="Collapsed" />
                                    </Grid>

                                    <Border Height="1" Background="{DynamicResource SettingsDivider}" Margin="0,0,0,8" />
                                    <Grid Margin="0,0,0,12">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                        </Grid.RowDefinitions>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="54" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="54" />
                                            <ColumnDefinition Width="62" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.ColumnSpan="3" Style="{StaticResource FieldLabel}" Text="窗口透明度" FontWeight="SemiBold" Foreground="{DynamicResource SettingsText}" />
                                        <TextBox x:Name="TxtOpacity" Grid.Column="3" Style="{StaticResource CompactValueBox}"
                                                 LostFocus="TxtOpacity_LostFocus" KeyDown="TxtOpacity_KeyDown" />
                                        <TextBlock Grid.Row="1" Grid.Column="0" Text="30%" Style="{StaticResource HintText}" VerticalAlignment="Center" HorizontalAlignment="Left" />
                                        <Slider x:Name="SliderOpacity" Grid.Row="1" Grid.Column="1" Style="{StaticResource SettingsSlider}"
                                                Minimum="30" Maximum="100" IsSnapToTickEnabled="True" TickFrequency="1"
                                                ValueChanged="SliderOpacity_ValueChanged" Margin="10,0" />
                                        <TextBlock Grid.Row="1" Grid.Column="2" Text="100%" Style="{StaticResource HintText}" VerticalAlignment="Center" HorizontalAlignment="Right" />
                                    </Grid>

                                    <Border Height="1" Background="{DynamicResource SettingsDivider}" Margin="0,0,0,8" />
                                    <Grid Margin="0,0,0,12">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                        </Grid.RowDefinitions>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="54" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="54" />
                                            <ColumnDefinition Width="62" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.ColumnSpan="3" Style="{StaticResource FieldLabel}" Text="字号大小" FontWeight="SemiBold" Foreground="{DynamicResource SettingsText}" />
                                        <TextBox x:Name="TxtFontSize" Grid.Column="3" Style="{StaticResource CompactValueBox}"
                                                 LostFocus="TxtFontSize_LostFocus" KeyDown="TxtFontSize_KeyDown" />
                                        <TextBlock Grid.Row="1" Grid.Column="0" Text="10 pt" Style="{StaticResource HintText}" VerticalAlignment="Center" HorizontalAlignment="Left" />
                                        <Slider x:Name="SliderFontSize" Grid.Row="1" Grid.Column="1" Style="{StaticResource SettingsSlider}"
                                                Minimum="10" Maximum="16" IsSnapToTickEnabled="True" TickFrequency="1"
                                                ValueChanged="SliderFontSize_ValueChanged" Margin="10,0" />
                                        <TextBlock Grid.Row="1" Grid.Column="2" Text="16 pt" Style="{StaticResource HintText}" VerticalAlignment="Center" HorizontalAlignment="Right" />
                                    </Grid>

                                    <Grid Margin="0,0,0,12">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="1.08*" />
                                            <ColumnDefinition Width="12" />
                                            <ColumnDefinition Width="0.92*" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Style="{StaticResource FieldLabel}" Text="字体" FontWeight="SemiBold" Foreground="{DynamicResource SettingsText}" Margin="0,0,18,0" />
                                        <ComboBox x:Name="CmbFontFamily" Grid.Column="1" Style="{StaticResource SettingsComboBox}" IsEditable="True"
                                                  MaxDropDownHeight="260" />
                                        <Grid Grid.Column="3">
                                            <TextBox x:Name="TxtFontSearch" Style="{StaticResource SettingsTextBox}"
                                                     TextChanged="TxtFontSearch_TextChanged"
                                                     ToolTip="输入字体名称" />
                                            <TextBlock x:Name="FontSearchPlaceholder"
                                                       Text="或输入字体名称"
                                                       Foreground="{DynamicResource SettingsMuted}"
                                                       Margin="12,0,0,0"
                                                       IsHitTestVisible="False"
                                                       VerticalAlignment="Center" />
                                        </Grid>
                                    </Grid>

                                    <Grid Margin="0,0,0,12">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="154" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Style="{StaticResource FieldLabel}" Text="字体颜色" FontWeight="SemiBold" Foreground="{DynamicResource SettingsText}" Margin="0,0,18,0" />
                                        <UniformGrid x:Name="FontColorPalette" Grid.Column="1" Columns="6" VerticalAlignment="Center" />
                                        <Grid Grid.Column="2" Margin="10,0,0,0">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="8" />
                                                <ColumnDefinition Width="34" />
                                            </Grid.ColumnDefinitions>
                                            <TextBox x:Name="TxtCustomFontColor" Style="{StaticResource SettingsTextBox}"
                                                     ToolTip="支持 #RRGGBB" Padding="12,0" MaxLength="7" />
                                            <Border Grid.Column="2"
                                                    Height="34"
                                                    CornerRadius="10"
                                                    BorderBrush="{DynamicResource SettingsBorder}"
                                                    BorderThickness="1"
                                                    Background="{DynamicResource SettingsInputBg}">
                                                <TextBlock Text="&#xE70F;" FontFamily="Segoe MDL2 Assets"
                                                           Foreground="{DynamicResource SettingsMuted}"
                                                           HorizontalAlignment="Center"
                                                           VerticalAlignment="Center"
                                                           IsHitTestVisible="False" />
                                            </Border>
                                        </Grid>
                                        <Button x:Name="BtnClearFontColor" Grid.Column="3" Content="恢复默认"
                                                Margin="10,0,0,0" Style="{StaticResource SecondaryButton}"
                                                Padding="12,6" Click="BtnClearFontColor_Click" />
                                    </Grid>

                                    <Border Height="1" Background="{DynamicResource SettingsDivider}" Margin="0,0,0,8" />
                                    <TextBlock Style="{StaticResource CardTitle}" Text="皮肤系统" Margin="0,0,0,8" />
                                    <UniformGrid Columns="4" Margin="0,0,0,8">
                                        <ToggleButton x:Name="TbSkinDefault" Margin="0,0,8,0" Padding="10,8" Style="{StaticResource SecondaryToggleButton}"
                                                      Click="SkinToggle_Click" Tag="default">
                                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                                <TextBlock Text="&#xE771;" FontFamily="Segoe MDL2 Assets" FontSize="13" Margin="0,0,8,0" VerticalAlignment="Center" />
                                                <TextBlock Text="默认（毛玻璃）" VerticalAlignment="Center" />
                                            </StackPanel>
                                        </ToggleButton>
                                        <ToggleButton x:Name="TbSkinSolid" Margin="0,0,8,0" Padding="10,8" Style="{StaticResource SecondaryToggleButton}"
                                                      Click="SkinToggle_Click" Tag="solid">
                                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                                <TextBlock Text="&#xE790;" FontFamily="Segoe MDL2 Assets" FontSize="13" Margin="0,0,8,0" VerticalAlignment="Center" />
                                                <TextBlock Text="纯色" VerticalAlignment="Center" />
                                            </StackPanel>
                                        </ToggleButton>
                                        <ToggleButton x:Name="TbSkinImage" Margin="0,0,8,0" Padding="10,8" Style="{StaticResource SecondaryToggleButton}"
                                                      Click="SkinToggle_Click" Tag="image">
                                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                                <TextBlock Text="&#xEB9F;" FontFamily="Segoe MDL2 Assets" FontSize="13" Margin="0,0,8,0" VerticalAlignment="Center" />
                                                <TextBlock Text="图片背景" VerticalAlignment="Center" />
                                            </StackPanel>
                                        </ToggleButton>
                                        <ToggleButton x:Name="TbSkinVideo" Padding="10,8" Style="{StaticResource SecondaryToggleButton}"
                                                      Click="SkinToggle_Click" Tag="video">
                                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                                <TextBlock Text="&#xE714;" FontFamily="Segoe MDL2 Assets" FontSize="13" Margin="0,0,8,0" VerticalAlignment="Center" />
                                                <TextBlock Text="视频背景" VerticalAlignment="Center" />
                                            </StackPanel>
                                        </ToggleButton>
                                    </UniformGrid>
                                    <RadioButton x:Name="RbSkinDefault" GroupName="Skin" Visibility="Collapsed" />
                                    <RadioButton x:Name="RbSkinSolid" GroupName="Skin" Visibility="Collapsed" />
                                    <RadioButton x:Name="RbSkinImage" GroupName="Skin" Visibility="Collapsed" />
                                    <RadioButton x:Name="RbSkinVideo" GroupName="Skin" Visibility="Collapsed" />
                                    <Grid x:Name="SkinImagePathRow" Margin="0,0,0,8" Visibility="Collapsed">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <TextBox x:Name="TxtSkinImagePath" Style="{StaticResource SettingsTextBox}" IsReadOnly="True" />
                                        <Button x:Name="BtnBrowseSkin" Grid.Column="1" Content="浏览图片..."
                                                Margin="8,0,0,0" Style="{StaticResource SecondaryButton}"
                                                Click="BtnBrowseSkin_Click" />
                                    </Grid>
                                    <Grid x:Name="SkinVideoPathRow" Margin="0,0,0,8" Visibility="Collapsed">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <TextBox x:Name="TxtSkinVideoPath" Style="{StaticResource SettingsTextBox}" IsReadOnly="True" />
                                        <Button x:Name="BtnBrowseSkinVideo" Grid.Column="1" Content="浏览视频..."
                                                Margin="8,0,0,0" Style="{StaticResource SecondaryButton}"
                                                Click="BtnBrowseSkinVideo_Click" />
                                    </Grid>
                                    <Grid Margin="0,0,0,12">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                        </Grid.RowDefinitions>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="54" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="54" />
                                            <ColumnDefinition Width="62" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.ColumnSpan="3" Style="{StaticResource FieldLabel}" Text="背景遮罩透明度" FontWeight="SemiBold" Foreground="{DynamicResource SettingsText}" />
                                        <TextBox x:Name="TxtSkinOverlay" Grid.Column="3" Style="{StaticResource CompactValueBox}"
                                                 LostFocus="TxtSkinOverlay_LostFocus" KeyDown="TxtSkinOverlay_KeyDown" />
                                        <TextBlock Grid.Row="1" Grid.Column="0" Text="0%" Style="{StaticResource HintText}" VerticalAlignment="Center" HorizontalAlignment="Left" />
                                        <Slider x:Name="SliderSkinOverlay" Grid.Row="1" Grid.Column="1" Style="{StaticResource SettingsSlider}"
                                                Minimum="0" Maximum="85" IsSnapToTickEnabled="True" TickFrequency="1"
                                                ValueChanged="SliderSkinOverlay_ValueChanged" Margin="10,0" />
                                        <TextBlock Grid.Row="1" Grid.Column="2" Text="85%" Style="{StaticResource HintText}" VerticalAlignment="Center" HorizontalAlignment="Right" />
                                    </Grid>

                                    <Border Height="1" Background="{DynamicResource SettingsDivider}" Margin="0,0,0,8" />
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                        </Grid.RowDefinitions>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="12" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="128" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Style="{StaticResource CardTitle}" Text="窗口大小" Margin="0" VerticalAlignment="Center" />
                                        <TextBlock Grid.Column="2" Style="{StaticResource HintText}"
                                                   Text="拖拽窗口四边或四角可调整大小"
                                                   VerticalAlignment="Center" />
                                        <Border Grid.Column="3" Height="42" CornerRadius="8" BorderBrush="{DynamicResource SettingsBorder}" BorderThickness="1"
                                                Background="{DynamicResource SettingsPreviewBg}" HorizontalAlignment="Right" Width="112">
                                            <Grid>
                                                <Border Margin="14,10" BorderBrush="{DynamicResource SettingsPreviewBorder}" BorderThickness="1" CornerRadius="4" />
                                                <TextBlock Text="&#xE71B;" FontFamily="Segoe MDL2 Assets" FontSize="10"
                                                           Foreground="{DynamicResource SettingsMuted}" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="6" />
                                            </Grid>
                                        </Border>
                                        <TextBlock Grid.Row="1" Style="{StaticResource FieldLabel}" Text="缩放方式"
                                                   FontWeight="SemiBold" Foreground="{DynamicResource SettingsText}"
                                                   VerticalAlignment="Center" Margin="0,12,18,0" />
                                        <StackPanel Grid.Row="1" Grid.Column="2" Grid.ColumnSpan="2" Orientation="Horizontal" Margin="0,12,0,0">
                                            <RadioButton x:Name="RbResizeUniform" Content="等比缩放" GroupName="ResizeMode" />
                                            <RadioButton x:Name="RbResizeFree" Content="自由缩放" GroupName="ResizeMode" />
                                        </StackPanel>
                                    </Grid>
                                </StackPanel>
                            </Border>

                            <Border x:Name="SectionModules" Height="0" Visibility="Collapsed" />
                            <Border Style="{StaticResource SettingsCard}" Padding="16,14">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="*" />
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="1.18*" />
                                        <ColumnDefinition Width="14" />
                                        <ColumnDefinition Width="1" />
                                        <ColumnDefinition Width="16" />
                                        <ColumnDefinition Width="1.16*" />
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.ColumnSpan="5" Orientation="Horizontal" Margin="0,0,0,12">
                                        <TextBlock Text="&#xECAA;" FontFamily="Segoe MDL2 Assets" FontSize="13"
                                                   Foreground="{DynamicResource SettingsMuted}" VerticalAlignment="Center" Margin="0,0,8,0" />
                                        <TextBlock Style="{StaticResource CardTitle}" Text="模块" Margin="0" />
                                    </StackPanel>

                                    <Grid Grid.Row="1" Grid.Column="0">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="*" />
                                        </Grid.RowDefinitions>
                                        <TextBlock Style="{StaticResource FieldLabel}" Text="显示模块" Margin="0,0,0,8" />
                                        <Grid Grid.Row="1">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="12" />
                                                <ColumnDefinition Width="58" />
                                            </Grid.ColumnDefinitions>
                                            <ListBox x:Name="ModuleOrderList" Height="176" Style="{StaticResource ModuleOrderListBox}"
                                                     SelectionChanged="ModuleOrderList_SelectionChanged" />
                                            <Grid Grid.Column="2">
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="Auto" />
                                                    <RowDefinition Height="Auto" />
                                                    <RowDefinition Height="*" />
                                                    <RowDefinition Height="Auto" />
                                                </Grid.RowDefinitions>
                                                <Button x:Name="BtnModuleUp" Grid.Row="0" Style="{StaticResource ModuleOrderButton}"
                                                        Click="BtnModuleUp_Click">
                                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                                        <TextBlock Text="&#xE70E;" FontFamily="Segoe MDL2 Assets" FontSize="9" Margin="0,0,4,0" VerticalAlignment="Center" />
                                                        <TextBlock Text="上移" />
                                                    </StackPanel>
                                                </Button>
                                                <Button x:Name="BtnModuleDown" Grid.Row="1" Style="{StaticResource ModuleOrderButton}"
                                                        Click="BtnModuleDown_Click">
                                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                                        <TextBlock Text="&#xE70D;" FontFamily="Segoe MDL2 Assets" FontSize="9" Margin="0,0,4,0" VerticalAlignment="Center" />
                                                        <TextBlock Text="下移" />
                                                    </StackPanel>
                                                </Button>
                                                <Button x:Name="BtnResetModuleOrder" Grid.Row="3" Content="恢复默认"
                                                        Style="{StaticResource ModuleOrderButton}" Click="BtnResetModuleOrder_Click" />
                                            </Grid>
                                        </Grid>
                                    </Grid>

                                    <Border Grid.Row="1" Grid.Column="2" Background="{DynamicResource SettingsDivider}" />

                                    <Border Grid.Row="1" Grid.Column="4" Background="{DynamicResource SettingsInnerPanelBg}" CornerRadius="8" Padding="12,12">
                                        <StackPanel>
                                            <TextBlock Style="{StaticResource CardTitle}" Text="番茄钟配置" Margin="0,0,0,14" />
                                            <Grid Margin="0,0,0,10">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="128" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Style="{StaticResource FieldLabel}" Text="专注时长" Foreground="{DynamicResource SettingsText}" />
                                                <Grid Grid.Column="1">
                                                    <TextBox x:Name="TxtPomodoroWork" Style="{StaticResource PomodoroValueBox}" />
                                                    <TextBlock Text="分钟" Foreground="{DynamicResource SettingsMuted}" HorizontalAlignment="Right" Margin="0,0,12,0" VerticalAlignment="Center" />
                                                </Grid>
                                            </Grid>
                                            <Grid Margin="0,0,0,10">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="128" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Style="{StaticResource FieldLabel}" Text="短休息时长" Foreground="{DynamicResource SettingsText}" />
                                                <Grid Grid.Column="1">
                                                    <TextBox x:Name="TxtPomodoroBreak" Style="{StaticResource PomodoroValueBox}" />
                                                    <TextBlock Text="分钟" Foreground="{DynamicResource SettingsMuted}" HorizontalAlignment="Right" Margin="0,0,12,0" VerticalAlignment="Center" />
                                                </Grid>
                                            </Grid>
                                            <Grid Margin="0,0,0,10">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="128" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Style="{StaticResource FieldLabel}" Text="长休息时长" Foreground="{DynamicResource SettingsText}" />
                                                <Grid Grid.Column="1">
                                                    <TextBox x:Name="TxtPomodoroLongBreak" Style="{StaticResource PomodoroValueBox}" />
                                                    <TextBlock Text="分钟" Foreground="{DynamicResource SettingsMuted}" HorizontalAlignment="Right" Margin="0,0,12,0" VerticalAlignment="Center" />
                                                </Grid>
                                            </Grid>
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="128" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Style="{StaticResource FieldLabel}" Text="长休息前番茄数" Foreground="{DynamicResource SettingsText}" />
                                                <Grid Grid.Column="1">
                                                    <TextBox x:Name="TxtPomodoroSessions" Style="{StaticResource PomodoroValueBox}" />
                                                    <TextBlock Text="个" Foreground="{DynamicResource SettingsMuted}" HorizontalAlignment="Right" Margin="0,0,12,0" VerticalAlignment="Center" />
                                                </Grid>
                                            </Grid>
                                        </StackPanel>
                                    </Border>
                                </Grid>
                            </Border>
                        </StackPanel>
                    </Grid>
                </ScrollViewer>
            </Grid>

            <Border x:Name="FooterBar" Grid.Row="2" Background="{DynamicResource SettingsFooterBg}" BorderBrush="{DynamicResource SettingsListBorder}" BorderThickness="0,1,0,0" Padding="18,10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Button x:Name="BtnRestoreDefaults" Style="{StaticResource SecondaryButton}"
                                Padding="12,9" Margin="0,0,14,0" Click="BtnRestoreDefaults_Click">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#xE777;" FontFamily="Segoe MDL2 Assets" FontSize="12" Margin="0,0,8,0" VerticalAlignment="Center" />
                                <TextBlock Text="恢复默认设置" VerticalAlignment="Center" />
                            </StackPanel>
                        </Button>
                        <Button x:Name="BtnOpenRepository" Style="{StaticResource LinkButton}"
                                Padding="8,6" Margin="0,0,10,0" Click="BtnOpenRepository_Click">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#xE774;" FontFamily="Segoe MDL2 Assets" FontSize="12" Margin="0,0,6,0" VerticalAlignment="Center" />
                                <TextBlock Text="关于" VerticalAlignment="Center" />
                            </StackPanel>
                        </Button>
                        <TextBlock x:Name="VersionText" Text="版本"
                                   FontSize="11" Foreground="{DynamicResource SettingsMuted}" VerticalAlignment="Center" />
                    </StackPanel>

                    <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="取消" Style="{StaticResource SecondaryButton}" Margin="0,0,14,0"
                                IsCancel="True" Click="Cancel_Click" />
                        <Button Content="应用" Style="{StaticResource SecondaryButton}" Margin="0,0,14,0"
                                Click="Apply_Click" />
                        <Button Content="保存设置" Style="{StaticResource PrimaryButton}" IsDefault="True"
                                Click="Save_Click" />
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>

