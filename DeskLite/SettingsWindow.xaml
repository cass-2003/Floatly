<Window x:Class="DeskLite.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        xmlns:wf="clr-namespace:System.Windows.Forms;assembly=System.Windows.Forms"
        xmlns:wfi="clr-namespace:System.Windows.Forms.Integration;assembly=WindowsFormsIntegration"
        Title="Floatly 设置"
        Width="1520"
        Height="980"
        MinWidth="1180"
        MinHeight="760"
        ShowInTaskbar="True"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        ResizeMode="CanResize"
        Background="#0B111A"
        Foreground="#FFFFFF"
        FontFamily="Microsoft YaHei UI"
        FontSize="13">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="0"
                            ResizeBorderThickness="6"
                            CornerRadius="0"
                            GlassFrameThickness="0"
                            UseAeroCaptionButtons="False" />
    </shell:WindowChrome.WindowChrome>

    <Window.Resources>
        <SolidColorBrush x:Key="SettingsBg" Color="#0B111A" />
        <SolidColorBrush x:Key="SettingsShellBg" Color="#F0121A28" />
        <SolidColorBrush x:Key="SettingsSidebarBg" Color="#C0111927" />
        <SolidColorBrush x:Key="SettingsCardBg" Color="#C21A2332" />
        <SolidColorBrush x:Key="SettingsCardBorder" Color="#1EFFFFFF" />
        <SolidColorBrush x:Key="SettingsText" Color="#F7FAFF" />
        <SolidColorBrush x:Key="SettingsMuted" Color="#9EACC0" />
        <SolidColorBrush x:Key="SettingsInputBg" Color="#F01A2435" />
        <SolidColorBrush x:Key="SettingsBorder" Color="#32FFFFFF" />
        <SolidColorBrush x:Key="SettingsAccent" Color="#4D82FF" />
        <SolidColorBrush x:Key="SettingsAccentDim" Color="#234D82FF" />
        <SolidColorBrush x:Key="SettingsGreen" Color="#55D38A" />
        <SolidColorBrush x:Key="SettingsRecord" Color="#F05252" />

        <Style x:Key="CardTitle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="{StaticResource SettingsText}" />
            <Setter Property="Margin" Value="0,0,0,14" />
        </Style>
        <Style x:Key="FieldLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource SettingsMuted}" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>
        <Style x:Key="InfoGlyph" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe MDL2 Assets" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Foreground" Value="{StaticResource SettingsMuted}" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>
        <Style x:Key="HintText" TargetType="TextBlock">
            <Setter Property="FontSize" Value="11" />
            <Setter Property="Foreground" Value="{StaticResource SettingsMuted}" />
            <Setter Property="TextWrapping" Value="Wrap" />
        </Style>

        <Style x:Key="SettingsCard" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource SettingsCardBg}" />
            <Setter Property="BorderBrush" Value="{StaticResource SettingsCardBorder}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="16" />
            <Setter Property="Padding" Value="18" />
            <Setter Property="Margin" Value="0,0,0,14" />
        </Style>

        <Style x:Key="SettingsTextBox" TargetType="TextBox">
            <Setter Property="Height" Value="34" />
            <Setter Property="Background" Value="{StaticResource SettingsInputBg}" />
            <Setter Property="Foreground" Value="{StaticResource SettingsText}" />
            <Setter Property="BorderBrush" Value="{StaticResource SettingsBorder}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="12,0" />
            <Setter Property="CaretBrush" Value="{StaticResource SettingsText}" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10"
                                Padding="{TemplateBinding Padding}"
                                SnapsToDevicePixels="True">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          VerticalAlignment="Stretch" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="SettingsComboBox" TargetType="ComboBox">
            <Setter Property="Height" Value="34" />
            <Setter Property="Background" Value="{StaticResource SettingsInputBg}" />
            <Setter Property="Foreground" Value="{StaticResource SettingsText}" />
            <Setter Property="BorderBrush" Value="{StaticResource SettingsBorder}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="12,6" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton"
                                          Focusable="False"
                                          IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          ClickMode="Press"
                                          Background="{TemplateBinding Background}"
                                          BorderBrush="{TemplateBinding BorderBrush}"
                                          BorderThickness="{TemplateBinding BorderThickness}">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                CornerRadius="10">
                                            <Grid>
                                                <TextBlock Text="&#xE70D;" FontFamily="Segoe MDL2 Assets"
                                                           Foreground="{StaticResource SettingsMuted}"
                                                           HorizontalAlignment="Right" VerticalAlignment="Center"
                                                           Margin="0,0,10,0" FontSize="10" />
                                            </Grid>
                                        </Border>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter x:Name="ContentSite"
                                              IsHitTestVisible="False"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}"
                                              Margin="12,0,32,0"
                                              VerticalAlignment="Center" />
                            <TextBox x:Name="PART_EditableTextBox"
                                     Style="{x:Null}"
                                     Background="Transparent"
                                     Foreground="{StaticResource SettingsText}"
                                     BorderThickness="0"
                                     Margin="12,0,32,0"
                                     VerticalContentAlignment="Center"
                                     Visibility="Hidden"
                                     IsReadOnly="{TemplateBinding IsReadOnly}" />
                            <Popup x:Name="Popup"
                                   Placement="Bottom"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   PopupAnimation="Fade">
                                <Border Background="#F01A2433"
                                        BorderBrush="{StaticResource SettingsBorder}"
                                        BorderThickness="1"
                                        CornerRadius="10"
                                        MinWidth="{TemplateBinding ActualWidth}"
                                        MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <ScrollViewer Margin="2" SnapsToDevicePixels="True">
                                        <ItemsPresenter />
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEditable" Value="True">
                                <Setter TargetName="PART_EditableTextBox" Property="Visibility" Value="Visible" />
                                <Setter TargetName="ContentSite" Property="Visibility" Value="Hidden" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="SettingsSlider" TargetType="Slider">
            <Setter Property="Minimum" Value="0" />
            <Setter Property="Maximum" Value="100" />
            <Setter Property="Height" Value="20" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Margin" Value="0,0,12,0" />
            <Setter Property="Foreground" Value="{StaticResource SettingsAccent}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Slider">
                        <Grid>
                            <Track x:Name="PART_Track">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="Slider.DecreaseLarge"
                                                  Background="{StaticResource SettingsAccent}"
                                                  BorderThickness="0"
                                                  Height="5"
                                                  VerticalAlignment="Center"
                                                  IsTabStop="False">
                                        <RepeatButton.Template>
                                            <ControlTemplate TargetType="RepeatButton">
                                                <Border Background="{TemplateBinding Background}" CornerRadius="2" />
                                            </ControlTemplate>
                                        </RepeatButton.Template>
                                    </RepeatButton>
                                </Track.DecreaseRepeatButton>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="Slider.IncreaseLarge"
                                                  Background="#354154"
                                                  BorderThickness="0"
                                                  Height="5"
                                                  VerticalAlignment="Center"
                                                  IsTabStop="False">
                                        <RepeatButton.Template>
                                            <ControlTemplate TargetType="RepeatButton">
                                                <Border Background="{TemplateBinding Background}" CornerRadius="2" />
                                            </ControlTemplate>
                                        </RepeatButton.Template>
                                    </RepeatButton>
                                </Track.IncreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb Width="16" Height="16">
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="Thumb">
                                                <Border Background="#F8FAFF" BorderBrush="#80FFFFFF" BorderThickness="1" CornerRadius="8" />
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="SettingsNumericBox" TargetType="TextBox" BasedOn="{StaticResource SettingsTextBox}">
            <Setter Property="Width" Value="72" />
            <Setter Property="TextAlignment" Value="Center" />
        </Style>
        <Style x:Key="SettingsCheckBox" TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource SettingsText}" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="MinHeight" Value="24" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <Border x:Name="Box"
                                    Width="18"
                                    Height="18"
                                    CornerRadius="4"
                                    Background="#1C2737"
                                    BorderBrush="#5A6A7F"
                                    BorderThickness="1"
                                    VerticalAlignment="Center">
                                <TextBlock x:Name="CheckGlyph"
                                           Text="&#xE73E;"
                                           FontFamily="Segoe MDL2 Assets"
                                           FontSize="10"
                                           Foreground="White"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"
                                           Visibility="Collapsed" />
                            </Border>
                            <ContentPresenter Margin="8,0,0,0" VerticalAlignment="Center" />
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Box" Property="Background" Value="{StaticResource SettingsAccent}" />
                                <Setter TargetName="Box" Property="BorderBrush" Value="{StaticResource SettingsAccent}" />
                                <Setter TargetName="CheckGlyph" Property="Visibility" Value="Visible" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.45" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="FloatlyToggle" TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource SettingsText}" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="MinHeight" Value="28" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <ContentPresenter Grid.Column="0" VerticalAlignment="Center" />
                            <Border x:Name="Track" Grid.Column="1" Width="38" Height="22" CornerRadius="11"
                                    Background="#2A3140" BorderBrush="#33FFFFFF" BorderThickness="1" Margin="12,0,0,0">
                                <Border x:Name="Thumb" Width="16" Height="16" CornerRadius="8"
                                        Background="#E9EEF7" HorizontalAlignment="Left" Margin="3,0,0,0" />
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Track" Property="Background" Value="{StaticResource SettingsAccent}" />
                                <Setter TargetName="Thumb" Property="HorizontalAlignment" Value="Right" />
                                <Setter TargetName="Thumb" Property="Margin" Value="0,0,3,0" />
                                <Setter TargetName="Thumb" Property="Background" Value="White" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.45" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="NavItemStyle" TargetType="ListBoxItem">
            <Setter Property="Padding" Value="16,11" />
            <Setter Property="Margin" Value="10,4" />
            <Setter Property="Foreground" Value="{StaticResource SettingsMuted}" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="Bd" Background="Transparent" CornerRadius="8" Padding="{TemplateBinding Padding}"
                                BorderThickness="2,0,0,0" BorderBrush="Transparent">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource SettingsAccentDim}" />
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource SettingsAccent}" />
                                <Setter Property="Foreground" Value="{StaticResource SettingsText}" />
                                <Setter Property="FontWeight" Value="SemiBold" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#18FFFFFF" />
                                <Setter Property="Foreground" Value="{StaticResource SettingsText}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource SettingsAccent}" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="22,10" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="MinWidth" Value="110" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="10" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#5A8DFF" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#3E72F0" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="#B51D2736" />
            <Setter Property="Foreground" Value="{StaticResource SettingsText}" />
            <Setter Property="BorderBrush" Value="{StaticResource SettingsBorder}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="14,8" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="MinWidth" Value="80" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#C21F2A3D" />
                                <Setter TargetName="Bd" Property="BorderBrush" Value="#46FFFFFF" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#D71A2333" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="SecondaryToggleButton" TargetType="ToggleButton">
            <Setter Property="Background" Value="#B51D2736" />
            <Setter Property="Foreground" Value="{StaticResource SettingsText}" />
            <Setter Property="BorderBrush" Value="{StaticResource SettingsBorder}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="10,7" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="MinWidth" Value="78" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="10"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource SettingsAccent}" />
                                <Setter TargetName="Bd" Property="BorderThickness" Value="2" />
                                <Setter TargetName="Bd" Property="Background" Value="#1E4D82FF" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#C21F2A3D" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="LinkButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{StaticResource SettingsMuted}" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="8,6" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="HorizontalContentAlignment" Value="Left" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="Transparent" Padding="{TemplateBinding Padding}">
                            <ContentPresenter />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Foreground" Value="{StaticResource SettingsText}" />
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="TitleButton" TargetType="Button" BasedOn="{StaticResource LinkButton}">
            <Setter Property="Width" Value="38" />
            <Setter Property="Height" Value="30" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>
        <Style x:Key="ModuleOrderListBox" TargetType="ListBox">
            <Setter Property="Background" Value="{StaticResource SettingsInputBg}" />
            <Setter Property="Foreground" Value="{StaticResource SettingsText}" />
            <Setter Property="BorderBrush" Value="{StaticResource SettingsBorder}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="4" />
        </Style>
        <Style x:Key="ModuleOrderButton" TargetType="Button" BasedOn="{StaticResource SecondaryButton}">
            <Setter Property="MinWidth" Value="58" />
            <Setter Property="Padding" Value="8,5" />
        </Style>
        <Style x:Key="HotkeyDisplayBox" TargetType="TextBox" BasedOn="{StaticResource SettingsTextBox}">
            <Setter Property="Height" Value="32" />
            <Setter Property="MinWidth" Value="136" />
            <Setter Property="Padding" Value="10,0" />
        </Style>
        <Style x:Key="UnitInputBox" TargetType="TextBox" BasedOn="{StaticResource SettingsTextBox}">
            <Setter Property="Height" Value="36" />
            <Setter Property="Padding" Value="14,0,52,0" />
        </Style>
        <Style x:Key="IconSquareButton" TargetType="Button" BasedOn="{StaticResource SecondaryButton}">
            <Setter Property="Width" Value="34" />
            <Setter Property="Height" Value="32" />
            <Setter Property="MinWidth" Value="34" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Background" Value="#1A2435" />
            <Setter Property="BorderBrush" Value="#3C4B62" />
        </Style>
        <Style TargetType="RadioButton">
            <Setter Property="Foreground" Value="{StaticResource SettingsText}" />
            <Setter Property="Margin" Value="0,0,26,0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <Grid Width="18" Height="18" VerticalAlignment="Center">
                                <Ellipse x:Name="Ring" Stroke="#6A778D" StrokeThickness="1.4" Fill="#162131" />
                                <Ellipse x:Name="Dot" Width="6" Height="6" Fill="White" Visibility="Collapsed" />
                            </Grid>
                            <ContentPresenter Margin="10,0,0,0" VerticalAlignment="Center" />
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Ring" Property="Fill" Value="{StaticResource SettingsAccent}" />
                                <Setter TargetName="Ring" Property="Stroke" Value="{StaticResource SettingsAccent}" />
                                <Setter TargetName="Dot" Property="Visibility" Value="Visible" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.45" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ScrollViewer">
            <Setter Property="Background" Value="Transparent" />
        </Style>
    </Window.Resources>

    <Border Background="{StaticResource SettingsShellBg}" BorderBrush="#26FFFFFF" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="44" />
                <RowDefinition Height="*" />
                <RowDefinition Height="58" />
            </Grid.RowDefinitions>

            <Grid x:Name="TitleBar" Grid.Row="0" Background="#B80D1521" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="18,0,0,0">
                    <TextBlock Text="&#xE713;" FontFamily="Segoe MDL2 Assets" FontSize="17" Foreground="#D8E5FF" Margin="0,0,12,0" />
                    <TextBlock Text="Floatly 2.0 - 设置" FontSize="15" Foreground="{StaticResource SettingsText}" VerticalAlignment="Center" />
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,10,0">
                    <Button Style="{StaticResource TitleButton}" Click="TitleMinimize_Click" ToolTip="最小化">
                        <TextBlock Text="&#xE921;" FontFamily="Segoe MDL2 Assets" />
                    </Button>
                    <Button Style="{StaticResource TitleButton}" Click="TitleMaximize_Click" ToolTip="最大化">
                        <TextBlock x:Name="MaximizeIcon" Text="&#xE922;" FontFamily="Segoe MDL2 Assets" />
                    </Button>
                    <Button Style="{StaticResource TitleButton}" Click="TitleClose_Click" ToolTip="关闭">
                        <TextBlock Text="&#xE8BB;" FontFamily="Segoe MDL2 Assets" />
                    </Button>
                </StackPanel>
            </Grid>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="184" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Background="{StaticResource SettingsSidebarBg}"
                        BorderBrush="#14FFFFFF" BorderThickness="0,0,1,0">
                    <Grid Margin="0,16,0,14">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <ListBox x:Name="NavList" Grid.Row="1" Background="Transparent" BorderThickness="0" Margin="0,4,0,0"
                                 ItemContainerStyle="{StaticResource NavItemStyle}"
                                 SelectionChanged="NavList_SelectionChanged">
                            <ListBoxItem Tag="general">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="&#xE713;" FontFamily="Segoe MDL2 Assets" FontSize="14" Margin="0,0,10,0" VerticalAlignment="Center" />
                                    <TextBlock Text="常规" VerticalAlignment="Center" />
                                </StackPanel>
                            </ListBoxItem>
                            <ListBoxItem Tag="appearance">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="&#xE790;" FontFamily="Segoe MDL2 Assets" FontSize="14" Margin="0,0,10,0" VerticalAlignment="Center" />
                                    <TextBlock Text="外观" VerticalAlignment="Center" />
                                </StackPanel>
                            </ListBoxItem>
                            <ListBoxItem Tag="weather">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="&#xE706;" FontFamily="Segoe MDL2 Assets" FontSize="14" Margin="0,0,10,0" VerticalAlignment="Center" />
                                    <TextBlock Text="天气" VerticalAlignment="Center" />
                                </StackPanel>
                            </ListBoxItem>
                            <ListBoxItem Tag="calendar">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="&#xE787;" FontFamily="Segoe MDL2 Assets" FontSize="14" Margin="0,0,10,0" VerticalAlignment="Center" />
                                    <TextBlock Text="日历" VerticalAlignment="Center" />
                                </StackPanel>
                            </ListBoxItem>
                            <ListBoxItem Tag="modules">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="&#xE80F;" FontFamily="Segoe MDL2 Assets" FontSize="14" Margin="0,0,10,0" VerticalAlignment="Center" />
                                    <TextBlock Text="模块" VerticalAlignment="Center" />
                                </StackPanel>
                            </ListBoxItem>
                        </ListBox>

                        <Button x:Name="BtnRestoreDefaults" Grid.Row="2" Style="{StaticResource SecondaryButton}"
                                Margin="14,10,14,0" Padding="12,9" Click="BtnRestoreDefaults_Click">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#xE777;" FontFamily="Segoe MDL2 Assets" FontSize="12" Margin="0,0,8,0" VerticalAlignment="Center" />
                                <TextBlock Text="恢复默认设置" VerticalAlignment="Center" />
                            </StackPanel>
                        </Button>

                        <StackPanel Grid.Row="3" Margin="20,18,18,0">
                            <TextBlock Text="关于 Floatly 2.0" FontSize="11" Foreground="{StaticResource SettingsMuted}" />
                            <TextBlock x:Name="VersionText" Text="版本 2.0.3" FontSize="11" Foreground="#66FFFFFF" Margin="0,2,0,0" />
                        </StackPanel>
                    </Grid>
                </Border>

                <ScrollViewer x:Name="ContentScrollViewer" Grid.Column="1"
                              Margin="18,16,18,0" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                    <Grid Width="{Binding ActualWidth, ElementName=ContentScrollViewer}" MaxWidth="1356" HorizontalAlignment="Left">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1.02*" MinWidth="324" />
                            <ColumnDefinition Width="16" />
                            <ColumnDefinition Width="1.02*" MinWidth="324" />
                            <ColumnDefinition Width="16" />
                            <ColumnDefinition Width="1.34*" MinWidth="428" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
<Border x:Name="SectionGeneral" Height="0" Visibility="Collapsed" />
                            <Border Style="{StaticResource SettingsCard}" Height="206" Padding="18,14,18,12">
                                <StackPanel>
                                    <TextBlock Style="{StaticResource CardTitle}" Text="窗口行为" />
                                    <Grid Margin="0,0,0,10">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="窗口置顶" Foreground="{StaticResource SettingsText}" VerticalAlignment="Center" />
                                        <CheckBox x:Name="ChkAlwaysOnTop" Grid.Column="1" Style="{StaticResource FloatlyToggle}" />
                                    </Grid>
                                    <Grid Margin="0,0,0,10">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="开机自动启动" Foreground="{StaticResource SettingsText}" VerticalAlignment="Center" />
                                        <CheckBox x:Name="ChkAutoStart" Grid.Column="1" Style="{StaticResource FloatlyToggle}" />
                                    </Grid>
                                    <Grid Margin="0,0,0,10">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                            <TextBlock Text="鼠标穿透（开启后无法拖动窗口）" Foreground="{StaticResource SettingsText}" VerticalAlignment="Center" />
                                            <TextBlock Text="&#xE946;" Style="{StaticResource InfoGlyph}" Margin="8,0,0,0" />
                                        </StackPanel>
                                        <CheckBox x:Name="ChkClickThrough" Grid.Column="1" Style="{StaticResource FloatlyToggle}" />
                                    </Grid>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                            <TextBlock Text="启用全局快捷键" Foreground="{StaticResource SettingsText}" VerticalAlignment="Center" />
                                            <TextBlock Text="&#xE946;" Style="{StaticResource InfoGlyph}" Margin="8,0,0,0" />
                                        </StackPanel>
                                        <CheckBox x:Name="ChkGlobalHotkey" Grid.Column="1" Style="{StaticResource FloatlyToggle}" />
                                    </Grid>
                                </StackPanel>
                            </Border>
                            <Border Style="{StaticResource SettingsCard}" Height="196" Padding="18,14,18,12">
                                <StackPanel>
                                    <TextBlock Style="{StaticResource CardTitle}" Text="工作时间" />
                                    <Grid Margin="0,0,0,10">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="112" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="上班时间" Foreground="{StaticResource SettingsText}" VerticalAlignment="Center" />
                                        <Border Grid.Column="1" HorizontalAlignment="Right" Width="148" Height="34"
                                                Background="{StaticResource SettingsInputBg}"
                                                BorderBrush="{StaticResource SettingsBorder}"
                                                BorderThickness="1"
                                                CornerRadius="10"
                                                Padding="10,4">
                                            <wfi:WindowsFormsHost x:Name="WorkStartTimeHost">
                                                <wf:DateTimePicker x:Name="WorkStartTimePicker"
                                                                   Format="Time"
                                                                   ShowUpDown="True"
                                                                   Width="126" />
                                            </wfi:WindowsFormsHost>
                                        </Border>
                                    </Grid>
                                    <Grid Margin="0,0,0,10">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="112" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="下班时间" Foreground="{StaticResource SettingsText}" VerticalAlignment="Center" />
                                        <Border Grid.Column="1" HorizontalAlignment="Right" Width="148" Height="34"
                                                Background="{StaticResource SettingsInputBg}"
                                                BorderBrush="{StaticResource SettingsBorder}"
                                                BorderThickness="1"
                                                CornerRadius="10"
                                                Padding="10,4">
                                            <wfi:WindowsFormsHost x:Name="WorkEndTimeHost">
                                                <wf:DateTimePicker x:Name="WorkEndTimePicker"
                                                                   Format="Time"
                                                                   ShowUpDown="True"
                                                                   Width="126" />
                                            </wfi:WindowsFormsHost>
                                        </Border>
                                    </Grid>
                                    <CheckBox x:Name="ChkOffWorkWeekdaysOnly"
                                              Style="{StaticResource SettingsCheckBox}"
                                              Content="仅在工作日显示下班倒计时" />
                                </StackPanel>
                            </Border>
                            <Border Style="{StaticResource SettingsCard}" Height="124" Padding="18,14,18,12">
                                <StackPanel>
                                    <TextBlock Style="{StaticResource CardTitle}" Text="时钟显示" />
                                    <Grid Margin="0,0,0,12">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="74" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="时间格式" Foreground="{StaticResource SettingsText}" VerticalAlignment="Center" />
                                        <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="24,0,0,0">
                                            <RadioButton x:Name="RbTime24h" Content="24 小时制" GroupName="TimeFormat" />
                                            <RadioButton x:Name="RbTime12h" Content="12 小时制" GroupName="TimeFormat" />
                                        </StackPanel>
                                    </Grid>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="显示秒" Foreground="{StaticResource SettingsText}" VerticalAlignment="Center" />
                                        <CheckBox x:Name="ChkShowSeconds" Grid.Column="1" Style="{StaticResource FloatlyToggle}" />
                                    </Grid>
                                </StackPanel>
                            </Border>
                            <Border x:Name="SectionWeather" Height="0" Visibility="Collapsed" />
                            <Border Style="{StaticResource SettingsCard}">
                                <StackPanel>
                                    <TextBlock Style="{StaticResource CardTitle}" Text="天气" />
                                    <UniformGrid Columns="2" Margin="0,0,0,10">
                                        <CheckBox x:Name="ChkShowWeather" Style="{StaticResource FloatlyToggle}" Content="显示天气" Margin="0,0,10,6" />
                                        <CheckBox x:Name="ChkShowCityName" Style="{StaticResource FloatlyToggle}" Content="城市名称" Margin="0,0,0,6" />
                                        <CheckBox x:Name="ChkShowSunrise" Style="{StaticResource FloatlyToggle}" Content="日出日落" Margin="0,0,10,0" />
                                        <CheckBox x:Name="ChkShowTomorrow" Style="{StaticResource FloatlyToggle}" Content="明日天气" />
                                    </UniformGrid>
                                    <TextBlock Style="{StaticResource CardTitle}" Text="城市定位" Margin="0,0,0,8" />
                                    <TextBlock Style="{StaticResource HintText}" Text="自动定位优先使用 Windows 位置服务；不可用时回退 IP 定位，也可手动输入城市。" Margin="0,0,0,8" />
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                        <RadioButton x:Name="RbAutoLocate" Content="自动定位城市" GroupName="CityLocate" />
                                        <RadioButton x:Name="RbManualCity" Content="手动输入城市" GroupName="CityLocate" Margin="16,0,0,0" />
                                    </StackPanel>
                                    <Grid Margin="0,0,0,10">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <TextBox x:Name="TxtCity" Style="{StaticResource SettingsTextBox}" />
                                        <Button x:Name="BtnDetectLocation" Grid.Column="1" Content="立即定位"
                                                Margin="8,0,0,0" Style="{StaticResource SecondaryButton}"
                                                Click="BtnDetectLocation_Click" />
                                    </Grid>
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                        <Ellipse Width="8" Height="8" Fill="{StaticResource SettingsGreen}" Margin="0,0,8,0" VerticalAlignment="Center" />
                                        <TextBlock x:Name="LocationStatusText" Style="{StaticResource HintText}" Text="定位状态：等待定位" VerticalAlignment="Center" />
                                    </StackPanel>
                                    <TextBlock x:Name="LocationIpWarningText" Style="{StaticResource HintText}"
                                               Text="代理可能影响 IP 定位结果，建议开启 Windows 位置服务。"
                                               Foreground="#FFB86C" Visibility="Collapsed" />
                                </StackPanel>
                            </Border>
                        </StackPanel>

<StackPanel Grid.Column="2">
                            <Border Style="{StaticResource SettingsCard}" Height="206" Padding="18,14,18,12">
                                <StackPanel>
                                    <TextBlock Style="{StaticResource CardTitle}" Text="快捷键设置" />
                                    <Grid Margin="0,0,0,10">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="132" />
                                            <ColumnDefinition Width="12" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="8" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="显示 / 隐藏快捷键" Foreground="{StaticResource SettingsText}" VerticalAlignment="Center" />
                                        <TextBox x:Name="TxtHotkeyShowHide" Grid.Column="2" Style="{StaticResource HotkeyDisplayBox}" IsReadOnly="True"
                                                 PreviewKeyDown="HotkeyTextBox_PreviewKeyDown"
                                                 GotFocus="HotkeyShowHide_GotFocus" LostFocus="HotkeyTextBox_LostFocus" />
                                        <Button x:Name="BtnRecordShowHide" Grid.Column="4" VerticalAlignment="Center"
                                                Style="{StaticResource IconSquareButton}"
                                                Click="BtnRecordShowHide_Click" ToolTip="编辑">
                                            <TextBlock Text="&#xE70F;" FontFamily="Segoe MDL2 Assets" />
                                        </Button>
                                    </Grid>
                                    <Grid Margin="0,0,0,10">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="132" />
                                            <ColumnDefinition Width="12" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="8" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="快速待办快捷键" Foreground="{StaticResource SettingsText}" VerticalAlignment="Center" />
                                        <TextBox x:Name="TxtHotkeyQuickTodo" Grid.Column="2" Style="{StaticResource HotkeyDisplayBox}" IsReadOnly="True"
                                                 PreviewKeyDown="HotkeyTextBox_PreviewKeyDown"
                                                 GotFocus="HotkeyQuickTodo_GotFocus" LostFocus="HotkeyTextBox_LostFocus" />
                                        <Button x:Name="BtnRecordQuickTodo" Grid.Column="4" VerticalAlignment="Center"
                                                Style="{StaticResource IconSquareButton}"
                                                Click="BtnRecordQuickTodo_Click" ToolTip="编辑">
                                            <TextBlock Text="&#xE70F;" FontFamily="Segoe MDL2 Assets" />
                                        </Button>
                                    </Grid>
                                    <Border BorderBrush="#18FFFFFF" BorderThickness="0,1,0,0" Margin="0,0,0,10" />
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="52" />
                                        </Grid.ColumnDefinitions>
                                        <StackPanel>
                                            <TextBlock Text="录制模式" Foreground="{StaticResource SettingsText}" Margin="0,0,0,4" />
                                            <TextBlock x:Name="HotkeyStatusText" Style="{StaticResource HintText}"
                                                       Text="点击录制后，按下新的快捷键..." />
                                        </StackPanel>
                                        <Button x:Name="BtnRecordMode" Grid.Column="1" Style="{StaticResource IconSquareButton}" Width="38" Height="34"
                                                Click="BtnRecordShowHide_Click" ToolTip="录制模式" HorizontalAlignment="Right" VerticalAlignment="Center">
                                            <Grid>
                                                <Ellipse Width="16" Height="16" Stroke="#F26A6A" StrokeThickness="2" />
                                                <Ellipse Width="6" Height="6" Fill="#F26A6A" />
                                            </Grid>
                                        </Button>
                                    </Grid>
                                </StackPanel>
                            </Border>
                            <Border Style="{StaticResource SettingsCard}" Height="334" Padding="18,14,18,12">
                                <StackPanel>
                                    <TextBlock Style="{StaticResource CardTitle}" Text="摸鱼收入助手" />
                                    <Grid Margin="0,0,0,10">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="126" />
                                            <ColumnDefinition Width="14" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="月薪设置（元）" Foreground="{StaticResource SettingsText}" VerticalAlignment="Center" />
                                        <TextBox x:Name="TxtMonthlySalary" Grid.Column="2" Style="{StaticResource UnitInputBox}" TextAlignment="Left" />
                                    </Grid>
                                    <Grid Margin="0,0,0,10">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="126" />
                                            <ColumnDefinition Width="14" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="每月工作天数" Foreground="{StaticResource SettingsText}" VerticalAlignment="Center" />
                                        <Grid Grid.Column="2">
                                            <TextBox x:Name="TxtWorkDaysPerMonth" Style="{StaticResource UnitInputBox}" />
                                            <TextBlock Text="天" Foreground="{StaticResource SettingsMuted}" HorizontalAlignment="Right" Margin="0,0,14,0" VerticalAlignment="Center" />
                                        </Grid>
                                    </Grid>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="126" />
                                            <ColumnDefinition Width="14" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="每日工作小时" Foreground="{StaticResource SettingsText}" VerticalAlignment="Center" />
                                        <Grid Grid.Column="2">
                                            <TextBox x:Name="TxtWorkHoursPerDay" Style="{StaticResource UnitInputBox}" />
                                            <TextBlock Text="小时" Foreground="{StaticResource SettingsMuted}" HorizontalAlignment="Right" Margin="0,0,14,0" VerticalAlignment="Center" />
                                        </Grid>
                                    </Grid>
                                </StackPanel>
                            </Border>
                            <Border x:Name="SectionCalendar" Height="0" Visibility="Collapsed" />
                            <Border Style="{StaticResource SettingsCard}">
                                <StackPanel>
                                    <TextBlock Style="{StaticResource CardTitle}" Text="日历" />
                                    <CheckBox x:Name="ChkShowWeekStrip" Style="{StaticResource FloatlyToggle}" Content="显示万年历" Margin="0,0,0,8" />
                                    <CheckBox x:Name="ChkShowHuangLi" Style="{StaticResource FloatlyToggle}" Content="显示黄历详情（宜忌、神煞、时辰）" Margin="0,0,0,10" />
                                    <TextBlock Style="{StaticResource FieldLabel}" Text="默认视图" Margin="0,0,0,8" />
                                    <StackPanel Orientation="Horizontal">
                                        <RadioButton x:Name="RbCalWeek" Content="周历" GroupName="Calendar" />
                                        <RadioButton x:Name="RbCalMonth" Content="月历" GroupName="Calendar" />
                                    </StackPanel>
                                </StackPanel>
                            </Border>
                        </StackPanel>

                        <StackPanel Grid.Column="4">
                            <Border x:Name="SectionAppearance" Height="0" Visibility="Collapsed" />
                            <Border Style="{StaticResource SettingsCard}">
                                <StackPanel>
                                    <TextBlock Style="{StaticResource CardTitle}" Text="主题" />
                                    <Grid Margin="0,0,0,14">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="1.2*" />
                                            <ColumnDefinition Width="14" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <Border x:Name="ThemeDarkCard" CornerRadius="12" BorderThickness="2" BorderBrush="{StaticResource SettingsAccent}"
                                                Background="#172238" Padding="14,12" Cursor="Hand" MouseLeftButtonUp="ThemeDarkCard_Click">
                                            <Grid MinHeight="62">
                                                <StackPanel VerticalAlignment="Center">
                                                    <Border Height="22" CornerRadius="7" Background="#111827" Margin="0,0,0,10" />
                                                <TextBlock Text="深色" Foreground="{StaticResource SettingsText}" VerticalAlignment="Bottom" />
                                                </StackPanel>
                                                <TextBlock x:Name="ThemeDarkCheck" Text="&#xE73E;" FontFamily="Segoe MDL2 Assets"
                                                           Foreground="{StaticResource SettingsAccent}" HorizontalAlignment="Right" VerticalAlignment="Bottom" />
                                            </Grid>
                                        </Border>
                                        <Border x:Name="ThemeLightCard" Grid.Column="2" CornerRadius="12" BorderThickness="2" BorderBrush="#24FFFFFF"
                                                Background="#E8ECF1" Padding="14,12" Cursor="Hand" MouseLeftButtonUp="ThemeLightCard_Click">
                                            <Grid MinHeight="62">
                                                <StackPanel VerticalAlignment="Center">
                                                    <Border Height="22" CornerRadius="7" Background="#F5F7FA" Margin="0,0,0,10" />
                                                <TextBlock Text="浅色" Foreground="#1F2937" VerticalAlignment="Bottom" />
                                                </StackPanel>
                                                <TextBlock x:Name="ThemeLightCheck" Text="&#xE73E;" FontFamily="Segoe MDL2 Assets"
                                                           Foreground="{StaticResource SettingsAccent}" HorizontalAlignment="Right" VerticalAlignment="Bottom"
                                                           Visibility="Collapsed" />
                                            </Grid>
                                        </Border>
                                        <RadioButton x:Name="RbThemeDark" GroupName="Theme" Visibility="Collapsed" />
                                        <RadioButton x:Name="RbThemeLight" GroupName="Theme" Visibility="Collapsed" />
                                    </Grid>

                                    <Grid Margin="0,0,0,6">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Style="{StaticResource FieldLabel}" Text="窗口透明度" />
                                        <Border Grid.Column="2" Background="#132131" BorderBrush="{StaticResource SettingsBorder}" BorderThickness="1" CornerRadius="8" Padding="10,4">
                                            <TextBlock Text="75%" Foreground="{StaticResource SettingsText}" FontWeight="SemiBold" />
                                        </Border>
                                    </Grid>
                                    <Grid Margin="0,0,0,4">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="30%" Style="{StaticResource HintText}" VerticalAlignment="Center" />
                                        <Slider x:Name="SliderOpacity" Grid.Column="1" Style="{StaticResource SettingsSlider}"
                                                Minimum="30" Maximum="100" IsSnapToTickEnabled="True" TickFrequency="1"
                                                ValueChanged="SliderOpacity_ValueChanged" Margin="16,0" />
                                        <TextBox x:Name="TxtOpacity" Grid.Column="2" Style="{StaticResource SettingsNumericBox}" Width="70"
                                                 LostFocus="TxtOpacity_LostFocus" KeyDown="TxtOpacity_KeyDown" />
                                    </Grid>

                                    <Grid Margin="0,10,0,6">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Style="{StaticResource FieldLabel}" Text="字号大小" />
                                        <Border Grid.Column="2" Background="#132131" BorderBrush="{StaticResource SettingsBorder}" BorderThickness="1" CornerRadius="8" Padding="10,4">
                                            <TextBlock Text="13 pt" Foreground="{StaticResource SettingsText}" FontWeight="SemiBold" />
                                        </Border>
                                    </Grid>
                                    <Grid Margin="0,0,0,10">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="10 pt" Style="{StaticResource HintText}" VerticalAlignment="Center" />
                                        <Slider x:Name="SliderFontSize" Grid.Column="1" Style="{StaticResource SettingsSlider}"
                                                Minimum="10" Maximum="16" IsSnapToTickEnabled="True" TickFrequency="1"
                                                ValueChanged="SliderFontSize_ValueChanged" Margin="16,0" />
                                        <TextBox x:Name="TxtFontSize" Grid.Column="2" Style="{StaticResource SettingsNumericBox}" Width="70"
                                                 LostFocus="TxtFontSize_LostFocus" KeyDown="TxtFontSize_KeyDown" />
                                    </Grid>

                                    <TextBlock Style="{StaticResource FieldLabel}" Text="字体" Margin="0,0,0,6" />
                                    <ComboBox x:Name="CmbFontFamily" Style="{StaticResource SettingsComboBox}" IsEditable="True" Margin="0,0,0,14" />

                                    <TextBlock Style="{StaticResource FieldLabel}" Text="字体颜色" Margin="0,0,0,6" />
                                    <Grid Margin="0,0,0,10">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <UniformGrid x:Name="FontColorPalette" Columns="6" />
                                        <TextBox x:Name="TxtCustomFontColor" Grid.Column="1" Style="{StaticResource SettingsTextBox}"
                                                 ToolTip="支持 #RRGGBB" Margin="10,0,0,0" />
                                        <Button Grid.Column="2" Style="{StaticResource SecondaryButton}" Padding="10,6" MinWidth="40" Margin="8,0,0,0"
                                                ToolTip="编辑">
                                            <TextBlock Text="&#xE70F;" FontFamily="Segoe MDL2 Assets" />
                                        </Button>
                                        <Button x:Name="BtnClearFontColor" Grid.Column="3" Content="恢复默认"
                                                Margin="8,0,0,0" Style="{StaticResource SecondaryButton}"
                                                Padding="12,6" Click="BtnClearFontColor_Click" />
                                    </Grid>

                                    <TextBlock Style="{StaticResource CardTitle}" Text="皮肤系统" Margin="0,2,0,8" />
                                    <UniformGrid Columns="4" Margin="0,0,0,8">
                                        <ToggleButton x:Name="TbSkinDefault" Margin="0,0,8,0" Padding="8,10" Style="{StaticResource SecondaryToggleButton}"
                                                      Click="SkinToggle_Click" Tag="default">
                                            <StackPanel HorizontalAlignment="Center">
                                                <TextBlock Text="&#xE771;" FontFamily="Segoe MDL2 Assets" FontSize="14" HorizontalAlignment="Center" />
                                                <TextBlock Text="默认" FontSize="10" Margin="0,2,0,0" HorizontalAlignment="Center" />
                                                <TextBlock Text="毛玻璃" FontSize="9" Foreground="{StaticResource SettingsMuted}" HorizontalAlignment="Center" />
                                            </StackPanel>
                                        </ToggleButton>
                                        <ToggleButton x:Name="TbSkinSolid" Margin="0,0,8,0" Padding="8,10" Style="{StaticResource SecondaryToggleButton}"
                                                      Click="SkinToggle_Click" Tag="solid">
                                            <StackPanel HorizontalAlignment="Center">
                                                <TextBlock Text="&#xE790;" FontFamily="Segoe MDL2 Assets" FontSize="14" HorizontalAlignment="Center" />
                                                <TextBlock Text="纯色" FontSize="10" Margin="0,2,0,0" HorizontalAlignment="Center" />
                                            </StackPanel>
                                        </ToggleButton>
                                        <ToggleButton x:Name="TbSkinImage" Margin="0,0,8,0" Padding="8,10" Style="{StaticResource SecondaryToggleButton}"
                                                      Click="SkinToggle_Click" Tag="image">
                                            <StackPanel HorizontalAlignment="Center">
                                                <TextBlock Text="&#xEB9F;" FontFamily="Segoe MDL2 Assets" FontSize="14" HorizontalAlignment="Center" />
                                                <TextBlock Text="图片" FontSize="10" Margin="0,2,0,0" HorizontalAlignment="Center" />
                                                <TextBlock Text="背景" FontSize="9" Foreground="{StaticResource SettingsMuted}" HorizontalAlignment="Center" />
                                            </StackPanel>
                                        </ToggleButton>
                                        <ToggleButton x:Name="TbSkinVideo" Padding="8,10" Style="{StaticResource SecondaryToggleButton}"
                                                      Click="SkinToggle_Click" Tag="video">
                                            <StackPanel HorizontalAlignment="Center">
                                                <TextBlock Text="&#xE714;" FontFamily="Segoe MDL2 Assets" FontSize="14" HorizontalAlignment="Center" />
                                                <TextBlock Text="视频" FontSize="10" Margin="0,2,0,0" HorizontalAlignment="Center" />
                                                <TextBlock Text="背景" FontSize="9" Foreground="{StaticResource SettingsMuted}" HorizontalAlignment="Center" />
                                            </StackPanel>
                                        </ToggleButton>
                                    </UniformGrid>
                                    <RadioButton x:Name="RbSkinDefault" GroupName="Skin" Visibility="Collapsed" />
                                    <RadioButton x:Name="RbSkinSolid" GroupName="Skin" Visibility="Collapsed" />
                                    <RadioButton x:Name="RbSkinImage" GroupName="Skin" Visibility="Collapsed" />
                                    <RadioButton x:Name="RbSkinVideo" GroupName="Skin" Visibility="Collapsed" />
                                    <Grid Margin="0,0,0,8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <TextBox x:Name="TxtSkinImagePath" Style="{StaticResource SettingsTextBox}" IsReadOnly="True" />
                                        <Button x:Name="BtnBrowseSkin" Grid.Column="1" Content="浏览图片..."
                                                Margin="8,0,0,0" Style="{StaticResource SecondaryButton}"
                                                Click="BtnBrowseSkin_Click" />
                                    </Grid>
                                    <Grid Margin="0,0,0,8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <TextBox x:Name="TxtSkinVideoPath" Style="{StaticResource SettingsTextBox}" IsReadOnly="True" />
                                        <Button x:Name="BtnBrowseSkinVideo" Grid.Column="1" Content="浏览视频..."
                                                Margin="8,0,0,0" Style="{StaticResource SecondaryButton}"
                                                Click="BtnBrowseSkinVideo_Click" />
                                    </Grid>
                                    <TextBlock Style="{StaticResource FieldLabel}" Text="背景遮罩透明度" Margin="0,0,0,6" />
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <Slider x:Name="SliderSkinOverlay" Style="{StaticResource SettingsSlider}"
                                                Minimum="0" Maximum="85" IsSnapToTickEnabled="True" TickFrequency="1"
                                                ValueChanged="SliderSkinOverlay_ValueChanged" />
                                        <TextBox x:Name="TxtSkinOverlay" Grid.Column="1" Style="{StaticResource SettingsNumericBox}" Width="70"
                                                 LostFocus="TxtSkinOverlay_LostFocus" KeyDown="TxtSkinOverlay_KeyDown" />
                                    </Grid>
                                    <TextBlock Style="{StaticResource HintText}" Text="0% - 85%" Margin="0,4,0,8" />

                                    <TextBlock Style="{StaticResource CardTitle}" Text="窗口大小" Margin="0,2,0,8" />
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="142" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Style="{StaticResource HintText}"
                                                   Text="拖拽窗口四边或四角可调整大小，超宽屏下内容保持最大宽度。"
                                                   VerticalAlignment="Center" />
                                        <Border Grid.Column="1" Height="52" CornerRadius="10" BorderBrush="{StaticResource SettingsBorder}" BorderThickness="1"
                                                Background="#121820" HorizontalAlignment="Right" Width="126">
                                            <Grid>
                                                <Border Margin="16,12" BorderBrush="#44FFFFFF" BorderThickness="1" CornerRadius="4" />
                                                <TextBlock Text="&#xE71B;" FontFamily="Segoe MDL2 Assets" FontSize="10"
                                                           Foreground="{StaticResource SettingsMuted}" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="6" />
                                            </Grid>
                                        </Border>
                                    </Grid>
                                </StackPanel>
                            </Border>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="1.08*" />
                                    <ColumnDefinition Width="14" />
                                    <ColumnDefinition Width="0.82*" />
                                </Grid.ColumnDefinitions>
                                <Border x:Name="SectionModules" Height="0" Visibility="Collapsed" />
                                <Border Style="{StaticResource SettingsCard}">
                                    <StackPanel>
                                        <TextBlock Style="{StaticResource CardTitle}" Text="模块" />
                                        <TextBlock Style="{StaticResource HintText}" Text="显示顺序（拖拽排序后续补齐，当前使用按钮调整）" Margin="0,0,0,6" />
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="58" />
                                            </Grid.ColumnDefinitions>
                                            <ListBox x:Name="ModuleOrderList" Height="104" Style="{StaticResource ModuleOrderListBox}"
                                                     SelectionChanged="ModuleOrderList_SelectionChanged" />
                                            <StackPanel Grid.Column="1" Margin="8,0,0,0">
                                                <Button x:Name="BtnModuleUp" Content="上移" Style="{StaticResource ModuleOrderButton}"
                                                        Click="BtnModuleUp_Click" Margin="0,0,0,8" />
                                                <Button x:Name="BtnModuleDown" Content="下移"
                                                        Style="{StaticResource ModuleOrderButton}" Click="BtnModuleDown_Click" Margin="0,0,0,8" />
                                                <Button x:Name="BtnResetModuleOrder" Content="重置"
                                                        Style="{StaticResource ModuleOrderButton}" Click="BtnResetModuleOrder_Click" />
                                            </StackPanel>
                                        </Grid>
                                    </StackPanel>
                                </Border>
                                <Border Grid.Column="2" Style="{StaticResource SettingsCard}">
                                    <StackPanel>
                                        <TextBlock Style="{StaticResource CardTitle}" Text="番茄钟配置" />
                                        <Grid Margin="0,0,0,6">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Style="{StaticResource FieldLabel}" Text="专注时长" />
                                            <TextBox x:Name="TxtPomodoroWork" Grid.Column="1" Style="{StaticResource SettingsNumericBox}" />
                                        </Grid>
                                        <Grid Margin="0,0,0,6">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Style="{StaticResource FieldLabel}" Text="短休息时长" />
                                            <TextBox x:Name="TxtPomodoroBreak" Grid.Column="1" Style="{StaticResource SettingsNumericBox}" />
                                        </Grid>
                                        <Grid Margin="0,0,0,6">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Style="{StaticResource FieldLabel}" Text="长休息时长" />
                                            <TextBox x:Name="TxtPomodoroLongBreak" Grid.Column="1" Style="{StaticResource SettingsNumericBox}" />
                                        </Grid>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Style="{StaticResource FieldLabel}" Text="长休息前番茄数" />
                                            <TextBox x:Name="TxtPomodoroSessions" Grid.Column="1" Style="{StaticResource SettingsNumericBox}" />
                                        </Grid>
                                    </StackPanel>
                                </Border>
                            </Grid>
                        </StackPanel>
                    </Grid>
                </ScrollViewer>
            </Grid>

            <Border Grid.Row="2" Background="#B80D1521" BorderBrush="#18FFFFFF" BorderThickness="0,1,0,0" Padding="18,10">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="取消" Style="{StaticResource SecondaryButton}" Margin="0,0,14,0"
                            IsCancel="True" Click="Cancel_Click" />
                    <Button Content="应用" Style="{StaticResource SecondaryButton}" Margin="0,0,14,0"
                            Click="Apply_Click" />
                    <Button Content="保存设置" Style="{StaticResource PrimaryButton}" IsDefault="True"
                            Click="Save_Click" />
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>
